# System Prerequisites (install separately):
# - yq: YAML parser (install with: brew install yq)
#
# Python Dependencies:
youtube-transcript-api>=0.6.0
pytube>=15.0.0
yt-dlp>=2023.0.0

# Installation:
# 1. Install system prerequisites: brew install yq
# 2. Install Python package: pip install -e .
# 3. This will provide CLI commands: transcript-extract, transcript-download

