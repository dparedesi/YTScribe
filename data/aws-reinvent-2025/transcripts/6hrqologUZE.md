---
video_id: 6hrqologUZE
video_url: https://www.youtube.com/watch?v=6hrqologUZE
is_generated: False
is_translatable: True
summary: In this comprehensive AWS re:Invent technical session, Giuseppe Angelo Porcelli and Arun Nagarajan demonstrate how Amazon SageMaker HyperPod CLI and SDK simplify the process of building, fine-tuning, and deploying AI models on persistent clusters designed specifically for foundation model workloads, addressing the complexity feedback from data scientists who found Kubernetes kubectl and YAML configuration management too cumbersome for generative AI workflows while providing both command-line and programmatic interfaces that abstract infrastructure details while maintaining full customization capabilities. SageMaker HyperPod operates as a resilient service that runs continuous health checks on cluster instances and automatically remediates failures through instance replacement or rebooting depending on whether GPU, networking, or other hardware issues are detected, achieving highly scalable deployments up to thousands of accelerators with preconfigured EFA networking for low-latency collective operations, single-spine node topology, managed auto-scaling support, and full stack customization from hardware choices through storage options like S3 or FSX for Lustre to framework selection including PyTorch, TensorFlow, and JAX, all orchestrated through either Slurm or Amazon EKS with Kubernetes APIs. The HyperPod CLI provides an open-source layered architecture where user experience through the "hype" command or SDK imports sits atop core modules that reuse SDK components to construct Kubernetes specifications from versioned templates representing custom resource definitions, enabling cluster creation through simplified config.yaml files with opinionated defaults rather than complex CloudFormation parameters, demonstrated through live coding that shows cluster setup, context configuration, and single-command operations for listing, validating, and deploying clusters. The training operator delivers superfast failure recovery that preserves container initialization work rather than tearing down containers during faults, custom monitoring capabilities that watch training job logs for stuck processes or loss curve anomalies beyond just infrastructure failures, and task governance integration for maximizing GPU utilization, demonstrated through fine-tuning a Llama 3 model on a tool-calling dataset where a manually triggered node failure recovered in just 6.5 seconds by rescheduling the pod to a different node without losing training progress or requiring lengthy reinitialization. The inference operator simplifies production deployment by automatically creating Application Load Balancers, managing SSL certificates and termination, configuring auto-scaling policies integrated with CloudWatch or Prometheus metrics, working seamlessly with Carpenter auto-scaling to provision instances on demand, and supporting models from FSX, S3, or SageMaker JumpStart through single-command deployment that handles all infrastructure setup, shown through deploying a custom model from S3 using a DJL container image and invoking the resulting SageMaker endpoint for chat completion inference. HyperPod task governance enables sophisticated resource management through cluster-level policies defining multiple priority classes where higher-priority workloads like inference can preempt lower-priority experimentation or training jobs, compute allocation per team corresponding to Kubernetes namespaces with guaranteed quotas for specific numbers of instances or granular GPU/memory/CPU resources, and borrowing/lending mechanisms that allow teams to utilize idle capacity from other teams' allocations until guaranteed workloads require those resources, demonstrated through scenarios where Team A borrows Team B's idle GPU capacity until Team B submits a guaranteed job that triggers preemption and suspension of Team A's borrowed workloads. The recently launched Spaces capability enables running interactive development environments including SageMaker Code Editor and JupyterLab directly on HyperPod clusters with fast startup through pre-cached container images, task governance integration allowing IDE workloads to be preempted by higher-priority training jobs, support for GPU fractions using Nvidia MIG technology for efficient resource sharing, and access through either VS Code remote connections or web UI interfaces that mount distributed file systems like FSX for seamless access to training data, checkpoints, and model artifacts across the development workflow.
keywords: SageMaker HyperPod, distributed training, Kubernetes orchestration, GPU cluster management, fault-tolerant AI infrastructure
---
