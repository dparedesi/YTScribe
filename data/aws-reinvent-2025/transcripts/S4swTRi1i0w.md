---
video_id: S4swTRi1i0w
video_url: https://www.youtube.com/watch?v=S4swTRi1i0w
is_generated: False
is_translatable: True
summary: "This session provides a comprehensive deep dive into how Amazon S3 is designed for high availability, examining both system-level and implementation-level approaches to handling failures. The presentation begins by defining availability as the ability to deal with failures, where failures can occur at multiple levels: individual hard drives can experience surface defects causing read failures or complete drive failures; servers can suffer from component failures like bad fans; racks can lose power; entire buildings can experience catastrophic events like fires; and failures can be either permanent component loss or transient unavailability due to power issues, networking problems, or component overload. S3's design goals include 99.99% availability and 11 nines of durability, with the critical addition of read-after-write consistency launched in 2020, which fundamentally changed how the system handles failures by requiring consistency guarantees that were previously acceptable to violate. The system-level architecture centers on the indexing subsystem, which holds all object metadata and is accessed on every data plane request, using a quorum-based algorithm where data is stored across replicas in separate availability zones to avoid single fault domain correlation, with reads and writes requiring only a majority of servers to succeed, allowing the system to continue operating even when individual nodes fail, and conflict resolution handled through timestamps. However, the caching layer introduced inconsistency before 2020, as cache nodes could route requests randomly, leading to situations where writes persisted to storage but reads could return stale cached values from different nodes, creating non-overlapping read and write paths that violated consistency. The solution implemented a cache coherency protocol using a replicated journal, a distributed data structure where nodes are chained sequentially so writes flow through nodes in order, creating well-defined ordering for mutations that enables sequence number assignment and watermark tracking, with a witness system that tracks high watermarks for index writes and allows cache nodes to determine if their cached values are stale without querying storage nodes. The journal architecture required dynamic reconfiguration to handle failures, as sequential forwarding meant a single node failure could halt the entire system, so nodes monitor each other's availability and request quorum-based configuration changes within milliseconds of failures, with the witness system also being quorum-based, ensuring the cache maintains failure allowance while achieving overlapping reads and writes. At the implementation level, failures must be understood as correlated events rather than isolated incidents, with physical failure domains ranging from individual hard drives to entire availability zones, and logical failure domains including software deployment groups where bugs affect all nodes with new software simultaneously. The system uses replication across multiple failure domains to ensure data remains available even when entire availability zones fail, with the design principle of exposing workloads to multiple failure domains so correlated failures don't impact all data copies. Fail-stop failures represent the simplest failure mode where components simply stop functioning, like power being disconnected, which is relatively easy to detect and react to, but becomes more complex with network components where some requests succeed while others fail depending on routing paths, requiring redundancy like multiple switches between availability zones and ring topologies that can route around failures by converting availability problems into latency problems. Crash consistency is a critical challenge in stateful systems, where fail-stop failures can leave systems in states unreachable during normal execution, such as files with only partial writes, requiring careful design to ensure systems return to consistent states after failures, which S3 addresses through Shodstor, their storage node software that helps engineers reason about reachable states in the presence of failures and concurrency. Gray failures represent a more insidious problem where systems appear to be functioning but are actually failing requests, such as front-end servers successfully receiving traffic but unable to reach downstream storage nodes, creating situations where the system is doing work but not useful work, making detection difficult. Retries are a powerful mechanism for handling gray failures by allowing requests to try different paths through the system, but they can create amplification problems in distributed systems where failures cascade through multiple service layers, each retrying multiple times, potentially creating 27 times more retries than original requests, requiring intentional retry strategy design with fewer retries deeper in the stack. Timeouts help address overload situations where servers become slow rather than completely failing, but they introduce congestive collapse scenarios where timed-out requests remain in server queues, causing servers to process abandoned work while clients have already retried elsewhere, creating a self-feeding cycle where every request times out and the server processes only abandoned requests. Solutions include processing queues from the back (last-in-first-out) to prioritize recent requests that haven't timed out yet, and implementing backoff strategies in retries to give servers time to recover, with metastable failure modes representing states where systems remain in degraded conditions long after the original problem has been resolved. Health checks enable automatic system healing by detecting failures and taking corrective actions like removing servers from DNS, but they require careful design to avoid single points of failure where a broken health check system could incorrectly mark all healthy servers as failed, necessitating holistic health views from multiple perspectives including same-region, cross-region, and internet-based checks to form comprehensive health assessments and detect correlated failures like networking issues that appear as server failures. The fundamental principle emphasized is that local systems should never make local decisions about service health, requiring global perspectives and correlation to avoid mistakes, illustrated through examples like hard drive health checking systems that must consult global rate limiters before taking actions, preventing runaway health check services from taking entire fleets out of service. The session concludes by emphasizing that availability design requires defining correctness goals, architecting systems to meet those goals through techniques like quorum-based systems and journals, and implementing components with careful consideration of how each piece can fail and how to remediate those failures without making local decisions about system health."
keywords: Amazon S3, High Availability, Quorum-based Systems, Cache Coherency, Failure Handling
---
