---
video_id: yvLmJS-ayyM
video_url: https://www.youtube.com/watch?v=yvLmJS-ayyM
is_generated: False
is_translatable: True
summary: "This session, \"Deep dive into advanced routing policy with AWS Cloud WAN (NET401),\" features AWS leaders Shridhar Kulkarni and Sid Vangala, along with Qi Jun from Airbnb, introducing new routing policy capabilities for AWS Cloud WAN that enable fine-grained control over route propagation, traffic steering, and path selection across hybrid and multi-region networks. Qi Jun shares Airbnb's comprehensive migration journey from a physical data center backbone built in 2013 to AWS Cloud WAN, explaining how the network engineering team rethought their architecture when their physical data center approached end-of-life in 2021, initially building a proof-of-concept with full-mesh Transit Gateway connections before evaluating Cloud WAN as a better fit due to its dynamic routing support, strong automation capabilities, and simpler infrastructure. The migration has been highly successful with production deployment for nearly two years, delivering multi-million dollar annual cost savings from decommissioning physical data centers and circuits, comprehensive automation enabling code-based network deployment and management with built-in version control and easy rollback, rapid deployment capabilities allowing new region data centers to be spun up in two days compared to weeks or months with physical infrastructure, and improved operational efficiency with better uptime, centralized monitoring, and reduced routine maintenance freeing teams to focus on strategic tasks. Shridhar Kulkarni then introduces the new routing policy capability, explaining that while Cloud WAN's fully dynamic routing eliminates static routes and automatically propagates routes across regions and segments, organizations need control over route propagations to minimize blast radius, deterministic traffic patterns for path preferences when multiple paths exist (SD-WAN vs Direct Connect), route scaling to prevent overwhelming routing limits on partner networks, and advanced visibility into routing databases for troubleshooting. The routing policy feature provides three core capabilities: route filtering using match-action philosophy to allow or drop prefixes based on prefix lists, CIDR ranges, BGP communities, AS path, or MED values in both inbound and outbound directions across attachments, segments, and regions; route summarization to advertise summary routes instead of multiple prefixes, preventing scaling issues while supporting global, per-region, or per-region-per-segment summarization strategies; and BGP attribute modification to manipulate local preference, MED values, BGP communities, and AS path (prepend, remove, replace) to influence path selection and traffic patterns. Policy definition follows a match-action approach where match conditions can include IPv4/IPv6 prefixes, CIDR ranges, prefix lists, AS numbers in AS path, BGP communities, or MED values, and actions include drop, allow, summarize, add/remove ASNs, add/remove communities, set MED values, or set local preference, with critical importance placed on policy directionality (inbound vs outbound) relative to the distribution point. Distribution points where policies can be applied include segment sharing (using routing policy references in share segment actions), cross-region route propagation (using new associate routing policy segment action with source and destination edge locations), and attachments (using routing policy labels that map policies to labels, which are then associated with attachments during creation). Detailed live demos illustrate practical use cases: route summarization where individual VPC prefixes (10.0.1.0/24, 10.0.2.0/24) are summarized to 10.0.0.0/14 when advertised to on-premises SD-WAN appliances, with best practices recommending per-region or per-region-per-segment summarization for Direct Connect to avoid non-optimal routing from equal-cost paths; filtering overlapping VPC CIDRs where a secondary non-overlapping CIDR is added and only the secondary CIDR is advertised on-premises while both primary and secondary remain available within AWS segments; cross-region compliance restrictions where European VPC routes are filtered from US regions using outbound route filters applied at cross-region segment sharing distribution points; hybrid route isolation using BGP communities where on-premises routes are tagged with different communities (65001:100 for production, 65001:200 for development) and route policies match and allow routes based on community tags when sharing between hybrid and production/development segments; and path preference optimization where local preference values are modified to prefer US West 2 for development traffic and US East 1 for production traffic when multiple paths exist. The session concludes with a demonstration of using AI assistants (Kiro) to generate routing policies through natural language prompts, showing how the AI analyzes existing Cloud WAN configuration, references AWS documentation, and generates policy JSON that can be reviewed and applied, highlighting the intersection of network automation and AI-powered development tools. Key considerations include requiring policy version 2025.11, CNE transitively passing all BGP communities across attachments, RIB presenting routes before routing policies are applied on local CNE, critical importance of policy directionality, unique prefix list requirements per CNE, limitations on BGP community match/modification for Direct Connect and transitive peering attachments, inability to replace or remove ASNs specified in CNE configuration, and routing policies not being supported for network function group service insertion which uses static routes that take priority."
keywords: AWS Cloud WAN, Routing Policy, BGP, Airbnb, Network Automation, Route Summarization, Route Filtering, Local Preference, BGP Communities, Hybrid Connectivity
---
