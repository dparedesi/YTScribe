---
video_id: 8ybMtzb6ytQ
video_url: https://www.youtube.com/watch?v=8ybMtzb6ytQ
is_generated: False
is_translatable: True
summary: This session, presented by Ron Ortloff (AWS Product Manager) and Yuri Zarubin (AWS Principal Software Engineer), explores what's new in Apache Iceberg V3 and provides forward-looking insights into V4 proposals, beginning with project history from its 2017 origins at Netflix through its 2018 Apache incubation, 2020 top-level project status, 2021 V2 spec release introducing merge-on-read capabilities, and the May 2025 ratification of the V3 spec, while clarifying the important distinction between specifications (contracts that must be voted on by the Project Management Committee) and releases (implementations that go through formal Apache Software Foundation release processes), noting that V3 spec features are being implemented across releases from 1.7 through 1.10, with the spec not yet 100% implemented in any single release. The presentation focuses on three top V3 features that customers are most interested in: variant data type, deletion vectors, and row lineage. The variant data type addresses semi-structured data support, enabling flexible schema handling for JSON data and IoT workloads where schema is best applied on read rather than upfront, with three key components including metadata for file pruning, encoding for applying data types to values, and shredding which extracts elements from variant data and materializes them as hidden columns with proper data types, statistics, and predicate pushdown capabilities, solving pain points where V2 users had to transform semi-structured data into fixed schemas, materialize additional columns, or store everything as strings with poor performance, with variant providing up to 4x better performance than structured types and up to 10x better than string parsing approaches. Deletion vectors represent a storage optimization feature that enhances the write modes available since V2, addressing the write amplification problem in copy-on-write mode where deleting one record from a 10-record table requires rewriting a new 9-record file, which becomes enormous across thousands of data files, while merge-on-read solves write amplification by writing delete files instead of rewriting data files but pays a read tax by requiring joins between delete files and data files, with V3 deletion vectors replacing V2 positional deletes by updating bitmap values and persisting them to disk as Puffin files rather than creating numerous small delete files, with the spec requiring only one delete file per snapshot and maintaining bitmaps efficiently during write operations, making deletion vectors ideal for GDPR compliance scenarios, data cleanup in medallion architectures, and incremental data pipelines with merge statements. Row lineage is a change tracking feature that's required and on by default in V3, with writers responsible for producing record-level change information using sequence numbers and row IDs, and readers automatically getting hidden columns (row ID and sequence number) on every V3 record, enabling incremental processing for data pipelines, event lifecycle tracking for orders or transactions, slowly changing dimensions, debugging by tracing calculation origins, and compliance capabilities to track modifications, with row lineage stored directly on records allowing time travel queries to see lineage state at any point in time, eliminating the compute cost of snapshot diffing required by V2 change logs and removing view maintenance complexity. Additional V3 features include core infrastructure improvements like default values stored in metadata rather than persisted in files, table encryption keys for granular encryption control with KMS integration and key rotation support, and multi-argument transformations for partitioning and sorting using multiple columns to better align with query patterns and avoid data skew, plus new data types including nanosecond timestamps for high-frequency temporal workloads, geography and geometry types following OGC standards for location and mapping queries, and the unknown type as a null placeholder for graceful schema evolution handling across engines. The session concludes with forward-looking V4 proposals focused on performance improvements: improved column statistics with proper structure to reduce memory pressure when engines deserialize large stat maps, adaptive metadata tree combining manifest list and manifest into a single structure with root and leaf nodes to skip one layer for small write use cases, and relative paths replacing absolute paths in manifests to enable table copying across buckets, accounts, or cloud storage providers without breaking metadata references, making features like S3 replication work seamlessly for data protection and backup scenarios.
keywords: Apache Iceberg, V3, Variant Data Type, Deletion Vectors, Row Lineage
---
