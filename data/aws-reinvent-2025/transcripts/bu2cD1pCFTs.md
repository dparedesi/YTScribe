---
video_id: bu2cD1pCFTs
video_url: https://www.youtube.com/watch?v=bu2cD1pCFTs
is_generated: False
is_translatable: True
summary: In this level 400 code-focused session, Amazon Bedrock specialists Danny and Akarsha walk through the end-to-end build of a hyper-personalized event assistant, using the project to illustrate modern agentic RAG patterns across knowledge bases, tools, memory, and production runtime. They begin by reframing retrieval augmented generation as broader context engineering that combines documents, curated tools, and user memory to give stateless agents precise information. After defining the goal—an agent that can both search conference sessions and remember user preferences—the presenters show how to create a simple agent in Strands Agents with a Haiku 4.5 model, then iteratively enrich it. They programmatically construct a knowledge base by downloading 583 re:Invent session documents, splitting them, storing them in S3 Vectors, creating a vector index sized for Titan Text Embeddings V2 (1024 dimensions), and configuring Bedrock Knowledge Bases with fixed-size chunking and retrieval APIs. A tool decorator exposes the knowledge base as an agent tool, enabling semantic search and retrieve-and-generate calls. They then address personalization via Agent Core Memory, demonstrating short-term and long-term storage and retrieval through lifecycle hooks (on agent initialization and on message added) that load user preferences into system prompts and persist new dialogue with actor and session IDs for separation and IAM-based access control. Hook providers and registries wire memory into the agent alongside the knowledge tool, and the state object carries deterministic actor/session IDs from the application rather than trusting model text. A live demo seeds the agent with “Alice” profile data, confirms preferences are extracted with contextual justifications, and shows recommendations tailored to interests in AI, security, and hands-on content. With the core functionality working locally, they pivot to productionizing on Agent Core Runtime for session-level isolation across compute, memory, and storage. They outline the minimal code changes—importing the Agent Core application, initializing it, defining an MCP-like entrypoint handler, and running the app—plus generating container artifacts via the starter toolkit to push to ECR, configure protocols, models, and environment variables, and launch serverless micro-VM sessions. Identity is layered in through Agent Core Identity and JWT authorizers (e.g., Cognito), propagating authorization headers so actor IDs derive from validated IDP tokens rather than hardcoded values, while session IDs align to runtime sessions. The talk closes with a recap of configuration snippets, observability tips, and shared resources, emphasizing guardrails, deterministic state, and secure scaling from a notebook proof of concept to an isolated, authenticated, production-ready agent capable of both retrieval and personalized memory.
keywords: agentic RAG, knowledge bases, agent memory, Bedrock, Strands Agents
---
