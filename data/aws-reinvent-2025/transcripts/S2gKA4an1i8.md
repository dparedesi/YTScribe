---
video_id: S2gKA4an1i8
video_url: https://www.youtube.com/watch?v=S2gKA4an1i8
is_generated: False
is_translatable: True
summary: This session, "AI-powered log analytics with Elastic Streams," addresses the challenges of traditional observability and introduces Elastic Streams as a solution for intelligent log management. The speaker highlights that current observability often struggles with context-less alerts, time-consuming data pipeline setup, and the loss of valuable log data. Traditional SREs find logs messy, unstructured, voluminous, and varied in format, leading to reliance on metrics and traces, which don't capture everything. Elastic Streams aims to leverage AI to extract insights from logs, providing a scalable, cost-effective platform. The session outlines Elastic's existing strengths: numerous integrations, high scalability, ML-driven pattern analysis, and powerful search/filter capabilities with ESQL (now supporting joins and ML functions). For efficient log storage, Elastic recently released LogsDB index mode, which uses compression, index sorting, and columnar data format (synthetic source) to reduce storage costs by up to 70%. A future compressed log processor will further reduce storage by templating repeated log lines. Elastic Streams, the new AI-powered log analytics engine, automates several steps: first, it identifies systems from logs using LLMs, then automatically partitions logs into sensible streams (e.g., Hadoop, Spark, order processing). Next, it extracts meaning by structuring unstructured log data, often using OpenTelemetry semantic conventions. Finally, it detects significant events by generating LLM-assisted queries for specific problems (e.g., Spark out-of-memory errors) and uses change point detection to alert on spikes or dips. It can also generate assets like dashboards and SLOs, and integrate with Elastic's vector database for RAG, accelerating root cause analysis. A demo showcases Elastic Streams' capabilities: automatically generating Grok patterns for unstructured trading data, partitioning logs (manually and via AI suggestions for Spark and Hadoop), structuring data for analytics, detecting specific problems (e.g., Spark out-of-memory exceptions) with AI-generated queries and alerts, and simplifying retention policy management and data quality monitoring (detecting failed or degraded documents due to parsing errors). The speaker emphasizes that Elastic Streams simplifies ingest, automates analysis, and allows SREs to focus on problem-solving rather than pipeline management, with a vision to extend this beyond logs to traces and metrics.
keywords: AI-powered Log Analytics, Elastic Streams, Observability, Log Management, Elastic Search, LLMs, Data Partitioning, Data Structuring, Anomaly Detection, Cost Optimization
---
