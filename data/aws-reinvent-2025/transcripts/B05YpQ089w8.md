---
video_id: B05YpQ089w8
video_url: https://www.youtube.com/watch?v=B05YpQ089w8
is_generated: False
is_translatable: True
summary: "Senior Principal Engineer Amrith presents an in-depth DynamoDB deep dive through two real customer case studies, starting with the explanation that DynamoDB is a document and key-value store delivering predictable low latency at any scale, routinely achieving over five nines availability with single-digit millisecond response times for foundational services like login systems, inventory management, and AWS control planes. The scale visualization demonstrates that at DynamoDB's request rate, one grain of rice per request would fill an Olympic swimming pool (17,000 bathtubs) in just 61 seconds, illustrating the massive throughput serving hundreds of customers at sustained rates exceeding 500,000 requests per second. The first case study involves a national polling application that needed to scale from 600 TPS to 45,000 TPS for a COVID-era mobile self-service system replacing physical polling locations, where the customer reported unexpected write throttling at only 800 TPS despite having properly scaled tables with high cardinality keys and small items ranging from 400 to 2,000 bytes. The root cause was traced to a pre-populated random ID table used to maintain voter anonymity, where the application scanned the table to retrieve random IDs for linking national IDs to personally identifiable information and survey data through separate PiD and SiD randomized identifiers with low-cardinality one-way hashes preventing reverse lookups. The critical issue was DynamoDB's deterministic hash function, which produces identical hash values for the same partition key across all tables, users, and regions, meaning that scanning the random ID table in hash order and writing those same values to the PII table caused sequential partition hammering rather than distributed horizontal scaling, as the hash avalanche property ensures small input changes produce large unpredictable hash changes but identical inputs always hash identically. The solution was prefixing the PiD with two characters like TX before writing to the PII table, leveraging the avalanche property to distribute write traffic across all partitions, enabling the team to successfully benchmark at 90,000 TPS (double the requirement) and conduct the survey without issues. The second case study examines a customer experiencing counterintuitive behavior where high traffic batch workloads achieved low flat-line latency while low continuous interactive traffic suffered high variable latency with a 2,000x peak-to-trough traffic ratio. Investigation revealed the architecture bottleneck was cache efficiency in request routers that handle authentication, authorization, metadata lookups, and rate limiting, where long-lived TCP connections to the same request router provide substantial caching benefits for SigV4 signatures, metadata, identity information, and KMS encryption keys required for decrypting data at rest on storage nodes. The customer's thousands of statically-provisioned containers maintained for static stability and peak capacity spread the low continuous traffic too thinly across the fleet, preventing effective cache warming and connection reuse, while high batch traffic concentrated requests through fewer active connections benefiting from fully warmed caches. The recommended solution was routing low continuous traffic to a small subset of hosts while maintaining the full fleet for batch workloads, successfully demonstrating in lab simulations that this configuration achieves consistently low latency across both traffic patterns while preserving static stability requirements for regulated industry compliance."
keywords: "DynamoDB, Hash Partitioning, Performance Optimization, Connection Pooling, Cache Efficiency"
---
