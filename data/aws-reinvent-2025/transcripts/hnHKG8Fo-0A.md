---
video_id: hnHKG8Fo-0A
video_url: https://www.youtube.com/watch?v=hnHKG8Fo-0A
is_generated: False
is_translatable: True
summary: Derek Martinez and Sabrina Petruzzo from AWS present a comprehensive live-coding session on building a secure nonprofit healthcare chatbot with robust data protection, focusing on four key security areas: data sanitization, prompt injection defenses, securing machine learning pipelines, and implementing a defense-in-depth strategy with six layers of security. The architecture implements encryption for data at rest and endpoints, fine-grained access control through IAM, comprehensive audit and defense systems using CloudTrail, automated compliance monitoring via AWS Config with HIPAA conformance packs, PII detection and data sanitization, and prompt injection defense mechanisms. The solution processes sensitive patient information through a SageMaker pipeline where internal data owners upload documents to an S3 bucket, triggering an automated processing pipeline that uses Amazon Textract to extract text from documents (handling both plain text files and PDFs/scanned images) and Amazon Comprehend to detect personally identifiable information, applying differential privacy techniques for data sanitization before storing cleaned data in a separate S3 bucket that powers the chatbot, ensuring raw unprocessed data never reaches the application layer. The live coding demonstration shows the implementation of a data protection Python file that handles text extraction from multiple file formats, implements differential privacy techniques including k-anonymity for ages (rounding to 10-year ranges) and randomization for birth years to prevent quasi-identifier attacks, uses Comprehend with configurable confidence scores (typically 80-90%) to detect various PII types including Social Security numbers, phone numbers, emails, and names, and applies appropriate masking or privacy protection based on PII type through a privacy protection router that intelligently routes different PII types to different protection mechanisms. The pipeline processes files in batch mode with graceful error handling that continues processing even if individual files fail, creates audit logs documenting what PII was detected and what privacy protections were applied, stores these logs in a separate audit bucket with CloudTrail monitoring access, and outputs cleaned files with a "cleaned" prefix to differentiate them from raw data. The chatbot interface demonstrates the system working correctly by answering queries like "how many patients have diabetes" with masked results showing no PII, applying differential privacy to age queries (showing ranges like 100-109 instead of exact ages), and successfully detecting and blocking prompt injection attacks when users attempt to override security settings, with the system logging alerts and triggering remediation workflows. The technical implementation uses SageMaker notebooks broken into six steps: installing dependencies, setting up security infrastructure (KMS encryption keys, IAM roles, HIPAA compliance rules), defining the SageMaker pipeline with processing environment and data protection steps, deploying the pipeline, verification with sample data, and execution that processes files from input buckets to output buckets. The session emphasizes practical considerations including the importance of region alignment for AWS services, confidence score tuning based on workload requirements, the ability to extend the pipeline for synthetic data generation using Glue jobs if needed, and the critical lesson that organizations must know what sensitive data their AI applications handle and what governance controls protect that information, with the presenters noting that prompt injection was identified as the top risk for large language models by the OWASP Foundation. The solution is designed specifically for nonprofit organizations that may lack large technical teams, providing production-ready code templates that can be deployed immediately, with the presenters encouraging attendees to take these templates and apply them to at least one AI workload to establish competitive advantage through proper security implementation.
keywords: AI security, data sanitization, PII detection, SageMaker pipeline, healthcare compliance
---
