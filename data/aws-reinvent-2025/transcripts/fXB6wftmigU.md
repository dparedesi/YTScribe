---
video_id: fXB6wftmigU
video_url: https://www.youtube.com/watch?v=fXB6wftmigU
is_generated: False
is_translatable: True
summary: This session presents optimization strategies for building high-performance applications requiring millions of requests per second with sub-millisecond latency using Amazon ElastiCache Serverless for Valkey, covering latency fundamentals, connection management, hotspot mitigation, and automatic scaling capabilities. The presentation begins with latency analysis comparing different storage and network layers where lab environments achieve around 100 microseconds round trips dominated by operating system overhead rather than network latency, memory access operates in tens of nanoseconds enabling hundreds or thousands of memory operations per request without significant impact, disk access provides less than one millisecond latency but with high variability due to multiple I/O operations across different blocks, traditional databases deliver 1-100 millisecond variable latency using memory and disk combinations making it difficult to guarantee memory-only queries, while Valkey exclusively uses memory providing predictable sub-millisecond latency where network becomes the dominant factor rather than Valkey itself. Network latency characteristics demonstrate same data center communication achieving 75-100 microsecond round trips primarily from operating system overhead, same availability zone communication reaching up to 1 millisecond since AZs contain multiple data centers miles apart, cross-AZ communication reaching milliseconds depending on distance bound by speed of light through fiber optics, with real-world measurements from US East 1 showing cross-AZ latencies far below 700 microseconds and intra-AZ latencies around or below 100 microseconds, all viewable through AWS Network Manager for application planning. Connection optimization practices emphasize persistent connections avoiding per-request connection establishment costs including DNS queries, TCP handshakes, TLS initialization, and cluster discovery, with persistent connections applicable even to Lambda functions that remain hot between invocations, while pipelining enables sending multiple requests on the same TCP connection without waiting for responses creating implicit pipelines in asynchronous code that increase throughput by reducing I/O operations. Head-of-line blocking challenges arise from Valkey's ordered protocol requiring responses in request order, where large responses block subsequent smaller responses, compounded by TCP packet loss requiring at least 200 microsecond retransmissions hardcoded in Linux kernel, creating significant impact when thousands of requests queue behind problematic requests, mitigated through connection pooling using multiple connections per application instance to limit concurrency per connection, reducing blast radius for large responses or packet loss, with strategies including dedicated connections for slower request types and application scaling to distribute load. Hotspot mitigation addresses scenarios where sufficient total cluster resources exist but traffic concentrates on single nodes due to hot keys, identifiable through network bandwidth metrics showing one shard significantly higher than others, with key duplication strategy creating multiple copies of popular objects as different keys distributed across shards enabling load balancing reads but requiring updates to all copies making it ideal for read-heavy workloads, while key splitting breaks large objects into smaller components like castle rooms enabling pipelined reads assembled on application side spreading load across more shards with potential for selective reads of object subsets. ElastiCache Serverless architecture automatically distributes infrastructure across multiple availability zones for high resiliency and performance, with client connections through VPC endpoints reaching Network Load Balancer routing to serverless proxy that routes requests to correct cache nodes using multiplexing technology with single TCP channel handling multiple client connections reducing connection count and system calls, while proxy ensures local AZ reads for minimum latency. Heat management service monitors cache load and physical host status across fleet in multi-tenant environment where cache nodes run within VMs on physical hosts with different sizes based on workload requirements, using power of two random choices algorithm selecting two hottest cache nodes and moving only the second one to avoid interrupting busiest nodes under heavy load while releasing resources, with proxy maintaining persistent connections during all changes moving connections to newly created cache nodes as needed. Instant scale-up capability handles burst workloads using platform technology without fixed memory or CPU footprint enabling instant rescaling in multi-tenant environment supporting 8 times more throughput on demand, while horizontal scale-out follows three stages including detection happening quickly with workload prediction capabilities, provisioning using warm pool of pre-installed cache nodes waiting for quick attachment, and data movement monitoring slots to determine hot versus cold data moving slots in parallel within slots and between targets enabling doubling cluster throughput every 2 minutes. Read from replica functionality allows flagging connections to read from replicas for latency-sensitive applications without strong consistency requirements, with proxy ensuring reads always occur from local availability zone regardless of primary or replica status achieving microsecond response times, while Valkey IO threads evolution demonstrates progression from single-threaded design achieving around 1,900 requests per second with 50% time busy on I/O, to IO threads offloading I/O requests doubling performance to nearly 4,000 requests per second with 50% time busy on processing commands, to dynamic IO threads architecture contributed by AWS where main thread orchestrates jobs spawning IO threads adjusted according to workload ensuring no race conditions with data affinity for cache locality, achieving over 1 million requests per second for single instance with ability to spawn and remove threads dynamically based on workload. Best practices include using persistent connections and connection pools since establishing new connections costs order of magnitude more than simple get/set commands, reading from replicas to scale read throughput and achieve high availability with microsecond response times for serverless, implementing key duplication for read-heavy workloads and key splitting for large objects, limiting object size to avoid consuming processing time and CPU while preventing network baseline exceedance, and using time-to-live expiry with random jitter to spread deletions across time windows preventing simultaneous deletion spikes that would make Valkey very busy.
keywords: ElastiCache Serverless, Valkey optimization, high-performance caching, connection pooling, hotspot mitigation
---
