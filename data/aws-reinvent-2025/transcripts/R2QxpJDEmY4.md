---
video_id: R2QxpJDEmY4
video_url: https://www.youtube.com/watch?v=R2QxpJDEmY4
is_generated: False
is_translatable: True
summary: |
  Arvind and William present AWS’s confidential computing strategy, emphasizing protection of sensitive data while in use and clarifying that AWS Nitro-based EC2 already delivers “always-on” isolation from cloud operators at no extra cost via a slim Nitro Hypervisor and offloaded virtualization functions on Nitro cards. They contrast classic virtualization with Nitro’s reimagined model to show how removing networking, storage, and management from the host yields near–bare-metal performance, default isolation from AWS staff, and a 27-page security design that NCC Group reviewed; service terms now state zero operator access. For customers needing protection even from their own operators, they introduce Nitro Enclaves: CPU/memory carved from an instance with no networking, storage, or admin access, communicating only over a secure local channel and using attestation documents signed by the Nitro Hypervisor to prove identity before KMS or another keystore releases secrets. Enclaves support all CPU families and flexible sizing, integrate natively with KMS, and rely on comparing a build-time reference hash to a runtime attestation hash to trust code. The talk then addresses AI-era needs—protecting proprietary models, weights, and user prompts on GPUs or accelerators—and unveils EC2 instance attestation, which brings enclave-like assurances to regular EC2 instances (including GPU/Inferentia/Trainium). The new pieces: attestable AMIs that produce a cryptographic hash of their contents, Nitro TPM attestation documents generated on launch, and KMS integration that accepts those documents, validates Nitro signatures and hash policies, and only returns data keys when measurements match. William outlines best practices for building an isolated compute environment: strip all interactive access (no SSH, serial console, or SSM), run only trusted minimal packages, enforce in-instance firewalls, make storage immutable/read-only, and expose only tightly authenticated, authorized APIs. A recipe using Amazon Linux, trusted code, and a Kiwi-built attestable AMI yields a reference hash; at runtime the Nitro TPM measures the instance (including attached accelerators), producing a calculated hash that KMS can compare to the trusted value before decrypting. They position this flow as the template for private inferencing, multi-party computation, tokenization, and federated learning: encrypt data, send it to an isolated EC2, have the instance present its attestation to KMS, receive a data key only if the measurement matches, process the payload (e.g., LLM inference or model-weight aggregation), and optionally encrypt outputs. The presenters reiterate why customers seek confidential computing—use cases like decryption, signing, tokenizing PII/financial assets, masking, or protecting models—and the dual threat model of shielding data from AWS and from their own admins. The session closes by reinforcing the “zero operator access” theme across Nitro Enclaves and EC2 attestation, the availability of these features in commercial regions and GovCloud, a playful reminder that “what happens in an enclave stays in an enclave,” and pointers to whitepapers, NCC validation, upcoming workshops, and code talks on attestation and trusted execution for generative AI so attendees can start building immediately.
keywords: confidential computing, Nitro Enclaves, EC2 attestation, KMS, AI security
---
