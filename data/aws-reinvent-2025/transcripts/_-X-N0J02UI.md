---
video_id: _-X-N0J02UI
video_url: https://www.youtube.com/watch?v=_-X-N0J02UI
is_generated: False
is_translatable: True
summary: This hands-on code demonstration showcases how to build production-grade AI agents using Amazon Bedrock Agent Core and Strands SDK to enhance legacy applications without modifying existing codebases, walking through progressive iterations from basic local agent creation to fully deployed cloud-based agents with memory, multi-session management, and integration with existing enterprise systems through MCP gateways. The presenters begin with foundational concepts explaining the agentic loop where prompts trigger agents acting as orchestrators that determine when to call foundation models for intelligence and when to invoke external tools for real-time data, database lookups, API calls, or code execution since models lack access to dynamic information like current weather conditions or internal business data, demonstrating this by creating a minimal Python agent with Strands SDK requiring only four lines of code to instantiate the agent class, capture user input from CLI, and process responses. The session progresses to adding HTTP request tools from Strands' built-in toolkit alongside system prompts establishing guardrails that constrain agent behavior to use only publicly accessible APIs without authentication requirements, then transforms the local CLI agent into a production-ready HTTP server listening on port 8080 with ping health check and invocations endpoints by adding the Bedrock Agent Core wrapper class and entry point decorator, enabling curl-based testing that reveals multi-iteration agentic loops where agents repeatedly interact with LLMs to discover appropriate APIs and construct responses over several seconds. Deployment automation leverages the Agent Core starter toolkit CLI that interactively configures YAML specifications including Python runtime version, execution roles with necessary IAM policies, S3 buckets for artifact storage, authorization methods supporting OAuth or IAM, memory configurations for short and long-term context retention, and deployment strategies choosing between simplified ZIP-based packaging comparable to Lambda deployments or Docker containers required for custom dependencies or non-Python languages like Java and Go that need wrapper implementations exposing mandatory endpoints. The live coding demonstration integrates a legacy Java-based ECS-hosted unicorn store application by first using Amazon Q Developer to automatically generate OpenAPI specifications from the existing codebase, then registering this schema as an Agent Core gateway target alongside Lambda functions and MCP servers, enabling the agent to discover and invoke legacy application endpoints like product listings, account creation, and shopping cart operations through gateway-mediated API translation without touching application code. Advanced implementation details cover session management requiring initialization methods that bind MCP clients to user sessions by authenticating with gateway OAuth providers to obtain access tokens, fetching available tools through list_tools_sync methods that retrieve targets and OpenAPI operation mappings, storing agents in session contexts to avoid re-initialization overhead on subsequent requests, and implementing lifespan managers that tie gateway clients to session lifecycles ensuring proper isolation between concurrent users. Memory integration adds agent_core_memory_session_manager configuration with session ID-based separation preventing cross-user information leakage, allowing agents to recall previous conversation context when users make follow-up requests like "buy the party dress" referring to earlier product discussions, streaming response parsing transforms raw JSON outputs into user-friendly text for improved end-user experiences, and comprehensive observability through distributed tracing visualizes application maps showing agent runtime invoking Bedrock LLMs eight times, accessing memory stores, calling OAuth secret managers, routing through gateways to unknown remote legacy services, all captured through CloudWatch with OpenTelemetry integration enabling performance analysis and debugging of multi-second agentic loops where agents iteratively reason about tool selection, API discovery, and response construction.
keywords: Agent Core, Strands SDK, MCP gateway, legacy modernization, session management
---
