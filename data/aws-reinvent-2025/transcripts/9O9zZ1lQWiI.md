---
video_id: 9O9zZ1lQWiI
video_url: https://www.youtube.com/watch?v=9O9zZ1lQWiI
is_generated: False
is_translatable: True
summary: This session, presented by Gunnar Giros (AWS Developer Advocate) and Alan Helton (AWS Serverless Hero), demonstrates how to build scalable self-orchestrating AI workflows using the Agent-to-Agent (A2A) protocol and Model Context Protocol (MCP) for autonomous multi-agent systems. The presentation addresses the challenge that generative AI agents don't follow linear playbooks and require dynamic collaboration at runtime, making traditional static orchestration inadequate. The solution uses choreography over central orchestration, where agents discover each other dynamically and collaborate without a central controller. The architecture is built on three core services: Lambda functions for isolated agent execution, Amazon Bedrock Agent Core for memory and observability, and Momento for fast coordination and caching. The fundamental execution model is the "agent loop" with four phases: compose (loading typed context through MCP tools and retrieving task state from Agent Core memory), query (calling the LLM through Bedrock with structured outputs), execute (performing actions like A2A invocations, API calls, or memory updates), and continue (deciding whether to persist state and iterate or complete). The system uses the REASON framework for building stable system prompts with role, instructions, steps, expectations, and narrowing sections to ensure deterministic behavior. Agent discovery is driven by A2A using "agent cards" (machine-readable JSON documents) that define capabilities, versions, and input/output schemas, allowing agents to discover and invoke each other at runtime without hard-coded registries. MCP provides standardized, schema-validated interfaces for agents to access tools and context, ensuring predictable data structures. The architecture distinguishes between supervisor agents (requiring reasoning-capable models like Nova Pro for global planning, dependency resolution, and output validation) and worker agents (using faster, cheaper models like Nova Canvas or Haiku for specific domain tasks). The multi-agent system follows microservices patterns with domain-scoped agents (e.g., payment agent, warehouse agent, order agent) that cannot cross domain boundaries, ensuring security and maintainability. Determinism is achieved through idempotency keys derived from task IDs, schema validation, atomic API calls, and consistent memory checks. The session includes demos showing a single-agent delivery rescheduling scenario and a complex multi-agent workflow handling a damaged package requiring refund processing, inventory allocation, order duplication, and payment finalization. Observability is provided through Bedrock's CloudWatch dashboard for metrics (invocation counts, token spend, latency) and A2A's task-based tracing capabilities, with Momento Topics enabling real-time subscription to agent execution events. The code examples demonstrate agent cards, REASON-based system prompts, MCP tool definitions, and supervisor-to-worker communication through A2A invocations, showing how supervisors use "invoke agent" tools to delegate work to worker agents, which launch new autonomous loops.
keywords: Agent-to-Agent Protocol, Multi-Agent Systems, Model Context Protocol, Autonomous Agents, AWS Bedrock, Agent Core, Choreography, Deterministic Workflows, Serverless Architecture, AI Orchestration
---
