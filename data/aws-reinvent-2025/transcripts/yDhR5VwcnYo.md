---
video_id: yDhR5VwcnYo
video_url: https://www.youtube.com/watch?v=yDhR5VwcnYo
is_generated: False
is_translatable: True
summary: This session provides comprehensive security best practices for serverless applications, covering foundational concepts through advanced identity-aware patterns for both traditional serverless APIs and emerging agentic AI applications. The presentation uses a fitness tracker application architecture as a practical example, demonstrating security considerations across API Gateway, Lambda functions, and DynamoDB. Foundational security principles begin with account boundaries separating development, testing, and production environments to enable fast iteration while maintaining production safety through structured deployment pipelines. Encryption considerations include KMS-encrypted Lambda environment variables, encrypted API Gateway caches, and default encryption in transit for all serverless services, with API Gateway exclusively accepting HTTPS requests. IAM security encompasses two planes: the control plane for AWS API calls managing resources, and the data plane for application runtime access, with Service Control Policies (SCPs) applying organization-wide compliance rules and resource-based policies controlling service-to-service access. Lambda functions require two distinct policy types: resource-based policies defining which services can invoke the function (like API Gateway), and execution roles controlling what the function can access during execution (like DynamoDB tables). Least privilege principles emphasize scoping policies to specific API actions rather than wildcards, with iterative permission refinement during development and automated verification in CI/CD pipelines. Permission boundaries provide additional developer access control, allowing developers to create policies while security teams limit effective permissions through deny rules, ensuring developers cannot grant themselves excessive access even with wildcard policies. SCPs enable enterprise-wide controls like restricting API Gateway creation to private endpoints only, enforcing compliance without individual role configuration. The session provides detailed coverage of OAuth 2.0 flows as the foundation for identity-aware serverless applications, explaining five key entities: resource owners (users), user agents (browsers), web applications (clients), protected APIs (resource servers), and authorization servers (identity providers like Amazon Cognito). The authorization code flow involves two phases: user interaction where users log in, approve access, and receive authorization codes, followed by machine-to-machine token exchange where applications use client IDs, client secrets, and authorization codes to obtain ID tokens (identifying users), access tokens (defining permitted actions through scopes), and refresh tokens (enabling automatic token renewal). Client credentials flow simplifies this for machine-to-machine scenarios by eliminating user interaction and directly exchanging client credentials for tokens. API Gateway authorizers validate tokens before invoking Lambda functions, with identity information available in Lambda context for fine-grained authorization decisions. Amazon Verified Permissions provides an alternative authorization mechanism using the Cedar policy language, enabling code-based policy definitions that specify allowed actions for specific user groups and resources, with Lambda authorizers querying Verified Permissions for allow/deny decisions. The session extends security considerations to agentic AI applications, explaining how tool calling enables agents to interact with serverless APIs through the Model Context Protocol (MCP), with security flows supporting both client credentials for machine-to-machine agent-to-API communication and code authorization flows for propagating end-user identity through agent layers to downstream APIs. AWS Bedrock Agent Core services simplify agent security through Agent Core Identity, which handles OAuth complexity automatically, supporting both IAM for AWS-native deployments and OAuth for cross-cloud scenarios, with inbound authorization (how users/applications access agents) and outbound authorization (how agents access resources) configured based on deployment context and target services. Key takeaways emphasize implementing least privilege policies with explicit resource and action specifications, applying defense-in-depth through multiple security layers including IAM roles, resource policies, WAF protection, and SCPs, leveraging AWS managed integrations to reduce operational burden, and automating security checks throughout the development lifecycle from code authoring through deployment. The session concludes that while building secure serverless applications requires careful consideration of multiple security dimensions, AWS provides comprehensive tooling and managed services that simplify implementation when following established patterns and best practices.
keywords: serverless security, OAuth 2.0, IAM policies, agentic AI security, API Gateway authorization
---
