---
video_id: _nOKPOTJqko
video_url: https://www.youtube.com/watch?v=_nOKPOTJqko
is_generated: False
is_translatable: True
summary: "This session addresses three major challenges in network operations at scale: manual processes that don't scale, inconsistent deployments creating security risks, and validation happening too late, applying automation, testing, and policy-driven deployment principles that development teams already use. The presentation demonstrates how to solve these challenges using AWS Organizations with Service Control Policies for governance, AWS Cloud WAN for building global networks with policy as code, and AWS Step Functions for orchestrating automation workflows. The use case focuses on an environment requiring controlled IP address management through VPC IPAM ensuring teams only create VPCs with IPs from approved IPAM pools, traffic segmentation with routing domains per environment (dev, prod, hybrid), traffic inspection between routing domains and internet egress, all while maintaining low operational overhead with only two engineers managing the entire network. The implementation begins with Service Control Policies that deny VPC creation unless using IPAM pools shared via AWS Resource Access Manager, preventing non-compliant VPCs with hardcoded CIDR blocks, then demonstrates creating compliant VPCs using IPAM pool allocation across multiple regions. AWS Cloud WAN policy as code enables building global networks with less than 50 lines of code, defining inspection between routing domains using service insertion to automatically route traffic through AWS Network Firewall inspection VPCs, configuring egress inspection for internet-bound traffic, implementing cross-region inspection with single-hop logic to avoid duplicate inspection, and automating VPC attachment association to routing domains based on organizational unit tags. The attachment policy logic automatically associates VPC attachments to their OU's corresponding routing domain and routes hybrid connections (VPNs, Direct Connect) to the hybrid segment, while Step Functions automation handles edge cases where incorrect domain tags might be applied, using EventBridge to capture Network Manager events for new VPC attachments, obtaining OU information from account metadata, and creating the correct domain tag from the networking account to ensure proper segment association. The session emphasizes architecting by use case rather than habit, starting with business outcomes and architecting for agility through infrastructure as code, modular designs, and continuous iteration, leveraging the full AWS portfolio including Organizations, Service Control Policies, Cloud WAN, Step Functions, and serverless services rather than relying on a single networking service. Key demonstrations include showing non-compliant VPC creation being blocked by SCPs, compliant VPC creation using IPAM pools, Cloud WAN policy document creation with routing policies and service insertion configurations, Step Functions state machine for automated tag creation, and live attachment association showing VPCs automatically moving from no segment to their designated routing domains. The solution enables network teams to focus on permission control and security architecture rather than blocking actions at the network level, grants application teams autonomy to work with their VPCs while ensuring compliance through guardrails, and provides a scalable foundation for managing networks across hundreds of accounts and multiple regions with minimal operational overhead."
keywords: AWS Cloud WAN, Network Automation, Service Control Policies, VPC IPAM, Policy as Code
---
