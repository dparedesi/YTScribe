---
video_id: oMVRvg_8m_I
video_url: https://www.youtube.com/watch?v=oMVRvg_8m_I
is_generated: False
is_translatable: True
summary: This session provides a comprehensive guide to DynamoDB data modeling using an accessible phone book library analogy that explains core concepts before diving into advanced scaling techniques. The presentation begins with the analogy where partition keys represent cities (like Las Vegas), sort keys represent alphabetically sorted names within each city book, and the base table functions like white pages with automatic replication across three availability zones for high availability. Local Secondary Indexes (LSIs) are compared to yellow pages providing alternative sort orders within the same partition with strong consistency, while Global Secondary Indexes (GSIs) enable different partition keys entirely (like zip codes) with eventual consistency. The session explains that DynamoDB uses hashing for partition key distribution to ensure even data spread across partitions, starting with four default partitions that automatically split when partitions reach approximately 10 gigabytes or when traffic exceeds partition limits of 3000 read capacity units and 1000 write capacity units per partition. The presentation demonstrates practical data modeling through a chatbot history application requiring storage of millions of users with threaded conversations, using user ID as the partition key and timestamp-thread ID combinations as sort keys to enable queries for all user threads, specific thread retrieval, and recent thread access. When requirements expand to query only thread metadata across all threads, the session shows how sparse GSIs solve this by projecting only items with specific attributes (metadata items) into the index, creating an affordable query pattern. The session transitions to enterprise-scale optimization techniques, emphasizing that smaller data reduces costs across storage, read/write units, point-in-time recovery, and backups. Compression using LZ4 or GZIP significantly reduces storage for large text fields, though it prevents database-level filtering. The presentation covers item splitting strategies where frequently updated attributes are separated into dynamic items to reduce write costs, with trade-off analysis showing when splitting becomes cost-effective based on update frequency. For soft delete patterns, the session demonstrates creating sparse GSIs with state-based partition keys (like "deleted") and timestamp sort keys, but warns about hot partition issues when delete rates exceed 1000 writes per second, introducing sharding techniques where partition keys include random suffixes (deleted#0 through deleted#19) to distribute load across multiple partitions. The session also shows alternative sharding patterns where shard numbers become partition keys and state values move to sort key prefixes, providing flexibility in query patterns. For background processing requirements like cost aggregation per model per day, the presentation introduces DynamoDB Streams as change data capture mechanism with exactly-once in-order delivery, integrated with Lambda for processing batches of changes while buffering updates to reduce write costs. The session warns about idempotency challenges when Lambda functions crash and restart, recommending techniques like message ID tracking or client request tokens with transactions. For full-text search requirements, the session explains that DynamoDB doesn't support search queries natively, recommending integration with Amazon OpenSearch Service through zero-ETL capabilities that automatically propagate DynamoDB changes to OpenSearch, enabling relevance-ordered search, analytics queries, and RAG applications. The final section covers disaster recovery for application-level data corruption, comparing full table restore at $150 per terabyte versus incremental export to S3 at 10 cents per gigabyte of log data, which allows selective rollback of specific item collections without creating duplicate tables, reducing recovery time from hours to minutes for large tables. Throughout the presentation, the speaker emphasizes that successful DynamoDB modeling requires understanding access patterns upfront, using indexes strategically to support different query requirements, and optimizing for cost through compression, item splitting, and efficient write patterns, while acknowledging that scaling from small applications to enterprise workloads requires careful consideration of partition limits, sharding strategies, and integration with complementary services for advanced use cases like search and analytics.
keywords: DynamoDB data modeling, partition keys, global secondary indexes, cost optimization, DynamoDB streams
---
