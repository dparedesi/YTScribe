---
video_id: FMxP-jvrd6U
video_url: https://www.youtube.com/watch?v=FMxP-jvrd6U
is_generated: False
is_translatable: True
summary: This comprehensive session on AWS Secrets Manager explores secrets management architecture patterns, deployment strategies at scale, and real-world customer implementation through Acquia's journey, beginning with an overview of Secrets Manager as AWS's purpose-built service for managing database credentials, application secrets, and other sensitive data beyond AWS credentials and encryption keys handled by IAM and KMS respectively, with core capabilities including default secure storage with KMS encryption, CloudTrail integration for complete audit trails, Lambda-based automated rotation configurable on organizational schedules, cross-region replication launched three years ago as the most-used feature enabling business continuity and disaster recovery, retrieval at scale supporting 10,000 TPS for get secret value operations, and the open-source Secrets Manager Agent for automated caching and TTL configuration to reduce API overhead. The presentation delves into architectural decision-making around centralized versus decentralized secrets management approaches, with AWS guidance evolving from recommending decentralization to recognizing that the optimal strategy depends on organizational requirements, compliance needs, and business objectives, examining three critical pillars: creation where centralized approaches use CI/CD pipelines, Terraform modules, or abstraction layers providing consistent naming, tagging, and access control policies but requiring upfront overhead and maintenance, while decentralized creation allows developers to use standard AWS SDKs and CLIs in application accounts with greater flexibility but potentially less consistency across accounts; lifecycle management where centralized rotation uses management account lambdas that can rotate secrets across multiple workload accounts requiring complex cross-account permissions but offloading rotation burden from developers and enabling easier compliance verification, while decentralized rotation gives application teams control over rotation windows and timing aligned with traffic patterns but requires aggregating logs for security visibility; and consumption or storage where centralized storage provides single-account control, easier security team management, and centralized logging but creates blast radius risks and prevents using managed rotation features like RDS one-click rotation, while decentralized storage places secrets in workload accounts enabling logical separation, easier access control, and better least-privilege implementation but requires centralized log aggregation. The session emphasizes that hybrid models combining centralized creation and management with decentralized storage are often most practical, providing consistency in secret creation and rotation while maintaining developer agility. Resilience is addressed through secret replicas enabling primary secrets in one region with replicas in multiple regions, each with its own ARN and resource policy but synchronized values, automatically updating when primary secrets rotate, supporting global workloads with regional failover capabilities. Acquia's customer journey demonstrates massive scale implementation with workloads across hundreds of AWS accounts spanning 12 regions, managing over 300,000 unique secret paths with three or more secret types per path, generating 400,000 to 100,000 Kubernetes external secret references per cluster, handling 3.2 million ephemeral pod events daily with 500 to 1000 pod events per cluster, resulting in hundreds of thousands of Secrets Manager API calls per hour with consistent cyclical patterns of 60,000 calls hourly. Acquia's architecture leverages a fronting API service managing secret type classification, RBAC controls, and business logic, integrating with external secrets operator and secret store CSI driver to deliver secrets into Kubernetes pods, implementing indirect access patterns to isolate customer secrets from platform secrets, maintaining least-privilege boundaries where customers only see their application secrets while platform secrets remain obfuscated, with emerging third class for AI agents requiring mixed access. Key learnings from Acquia include the importance of simple usage interaction patterns, indirect access as security foundation for untrusted code like AI agents, critical need for clear naming conventions and hierarchical structure enabling unique secret type identification, and smart caching strategies to reduce API calls and costs while improving performance, recognizing that IAM serves as AWS security backbone but Secrets Manager fills gaps for interconnecting services and solving multi-tenant capabilities. Recent launches include EKS add-on for Secrets Manager CSI provider enabling one-click installation and automatic configuration eliminating manual setup overhead, and managed external secrets extending one-click rotation capabilities to third-party secrets like Salesforce, eliminating the need for custom Lambda functions per partner by using partner-provided rotation modules executed automatically based on configurable schedules, solving availability risks from mismatched rotations between Secrets Manager and external systems, with Secrets Manager now integrating with 55-plus AWS services managing customer secrets behind the scenes with one-click rotation functionality, representing AWS's commitment to eliminating human-visible secrets and making secure secret storage the default choice for cloud workloads.
keywords: AWS Secrets Manager, secrets management architecture, centralized vs decentralized, secret rotation, Kubernetes secrets
---
