---
video_id: 6autfsn1fy8
video_url: https://www.youtube.com/watch?v=6autfsn1fy8
is_generated: False
is_translatable: True
summary: This session, "Building AI Agent Experiences with Agent Core Gateway and EKS," features Roland Barcia and Carlos Santana, focusing on how to expose existing containerized microservices as tools for AI agents using AWS Agent Core Gateway and Amazon EKS. Roland, an agent developer, and Carlos, a Kubernetes platform engineer, address the challenge of integrating years of existing deterministic business logic, often locked within microservices on Kubernetes, into the new agentic AI paradigm. The session highlights Agent Core as a serverless runtime for agents, leveraging components like Agent Core Identity for secure access and Agent Core Gateway to expose tools. EKS is emphasized as a robust Kubernetes platform, with new features like EKS capabilities (fully managed Argo CD, Crossplane, ACK) and hosted EKS MCP servers for agentic DevOps. The proposed architecture involves a chat UI interacting with an Agent Core runtime application, which in turn uses Agent Core Gateway to access existing microservices on an EKS cluster. Bedrock is used for LLM inference. Carlos demonstrates the platform engineering side, showing an EKS cluster (using EKS auto-mode with Karpenter for dynamic node provisioning and BottleRocket OS for security) and three microservices (appointments, customer, technician) deployed via Kubernetes manifests with ingress for external exposure. These microservices have OpenAPI (Swagger) specifications, which are crucial for Agent Core Gateway. The setup process involves creating an IAM role for the MCP Gateway, securing it with Cognito for inbound authentication, and creating an identity provider in Agent Core to handle token exchange for agents. The OpenAPI JSON files for each microservice are modified to include the service URL and then uploaded to an S3 bucket, from which Agent Core Gateway creates targets, effectively converting the REST APIs into MCP tools. Roland then demonstrates the agent developer's perspective using the Qiro IDE. His agent code retrieves SSM parameters for gateway configuration, uses a Python decorator for secure, refreshed authorization tokens, and initializes an MCP client. The agent is built using the Strands SDK, given the gateway tools, an LLM (Anthropic's Haiku 4.5), and system prompts, then wrapped as an A2A (Agent-to-Agent) server within Agent Core runtime. The live demo shows the chat UI interacting with the agent: first, asking what tools are available (listing the three microservices), then simulating a refrigerator emergency (Carlos's salsa dancing incident) to create a high-urgency claim and schedule an emergency service appointment, demonstrating the agent's ability to orchestrate actions across the exposed microservices. The agent also intelligently responds to out-of-scope requests (e.g., "What to do about Carlos?"). The session concludes by emphasizing the ability to expose existing deterministic logic, the security benefits of Agent Core Identity, and the modernization potential of agentic AI for automating business processes.
keywords: AWS Agent Core Gateway, EKS, AI Agents, Microservices, Model Context Protocol, MCP Gateway, Kubernetes, OpenAPI, Swagger, Qiro, Agent Core Identity, Serverless Runtime, Bedrock, Automation
---
