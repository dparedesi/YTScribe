---
video_id: hSkElcxjdfs
video_url: https://www.youtube.com/watch?v=hSkElcxjdfs
is_generated: False
is_translatable: True
summary: "This session challenges the perception of serverless as magical and problem-free, emphasizing that failures are inevitable in distributed systems and require conscious design. The talk begins by contrasting traditional monolithic development with the complexities introduced by cloud and serverless architectures, explaining how serverless abstracts away infrastructure but doesn't eliminate underlying distributed system failures, which instead manifest in less obvious ways. The core message establishes that distributed systems are collections of machines connected by a network, and both machines and networks are prone to independent, non-deterministic failures, with cloud providers managing this complexity while developers building applications on top must still account for it. The presenter shares a personal story of losing data in a Kinesis-Lambda pipeline due to unhandled failures, highlighting the dangers of blindly trusting defaults and demonstrating how higher levels of abstraction can mask critical issues until they cause complete system outages. The session introduces timeouts and retries as hidden superpowers for resilience, but warns they can backfire catastrophically if not carefully configured, explaining how retries without exponential backoff and jitter can amplify small glitches into cascading failures that consume resources and overwhelm downstream systems. The AWS SDK handles retryable errors with exponential backoff, but developers must be mindful of their configuration, understanding that retries are inherently selfish and can cause ripple effects of cascading failures through systems, ultimately bringing entire architectures down if not managed properly. Timeouts are crucial to prevent indefinite waiting, but default infinite timeouts in JavaScript SDK v3 are dangerous, with too-long timeouts exhausting resources while too-short ones can trigger premature retries that increase load and costs, creating a match made in hell when paired with retries. The session addresses service limits and throttling, explaining that no resource scales infinitely despite promises of scalability, with Kinesis shard limits of 1MB or 1000 records per second leading to throttling when exceeded. Crucially, batch operations like Kinesis PutRecords can experience partial failures where some records succeed and others fail, but the SDK might return success, requiring developers to explicitly handle these partial failures and retry only the failed records with exponential backoff and jitter to avoid data loss and system overload. The poison pill record scenario is detailed, where a single bad record can cause Lambda to endlessly retry a batch, blocking an entire Kinesis shard for up to 24 hours and leading to massive data loss as records expire unprocessed, with the shard becoming completely non-functional and unable to process subsequent valid data. To mitigate this, Lambda's event source mapping error handling capabilities including timeouts, retry limits, and on-failure destinations must be configured away from dangerous defaults that set both to negative one, meaning no limits, which causes the exact problems demonstrated in the failure story. Another failure mode discussed is Lambda concurrency limits, defaulting to 1000 per region and account, where one Lambda consumer can monopolize capacity and affect unrelated Lambdas across the entire account, demonstrating how service limits can have blast radius well beyond individual architectures. The key takeaways emphasize understanding workload architecture, making conscious decisions about failure handling, never blindly trusting defaults for timeouts and retries, understanding service limits and partial failures, and continuously learning from failures to build more resilient systems that embrace chaos rather than pretending it doesn't exist."
keywords: distributed systems, serverless resilience, timeouts, retries, service limits
---
