---
video_id: -7B31Edtn5M
video_url: https://www.youtube.com/watch?v=-7B31Edtn5M
is_generated: False
is_translatable: True
summary: "In this technical deep dive, Pavan (Principal Serverless Specialist) and Diego (Principal Engineer, Step Functions) explore advanced patterns for building and monitoring high-scale serverless workflows using AWS Step Functions. The session opens with a practical demonstration of the \"Distributed Map\" state, processing a massive open-source wind speed dataset containing nearly 600,000 S3 objects (partitioned by year and station, totaling ~70GB). Pavan guides the audience through the console setup, configuring a Distributed Map to process items in batches of 500 with a concurrency of 1000. He contrasts this with \"Inline Map,\" emphasizing that Distributed Map spawns a separate child execution for each iteration, allowing for massive parallelism (up to 10,000 concurrent executions) and better fault tolerance. The workflow uses a Lambda function to calculate mean wind speeds in a map-reduce pattern, transforming units from knots to MPH on the fly. Shifting to observability, Diego addresses the challenges of monitoring these asynchronous, high-volume operations. He introduces three critical new CloudWatch metrics: OpenMapRunLimit, OpenMapRunCount, and MapRunBacklogSize. He explains that because Step Functions is designed to be resilient, it doesn't fail immediately when throttled; instead, it queues executions. Without visibility, a growing backlog can silently delay processing. A live load-test demo visualizes these metrics, showing exactly when the system hits account limits and how the backlog accumulates. The duo also discusses \"State Transition\" quotas (defaulting to 5000/sec in major regions), explaining the underlying token bucket algorithm. They show how to build a granular CloudWatch dashboard to identify \"noisy neighbor\" state machines that might be consuming the shared account-level throughput capacity. The final segment tackles the complexities of cross-account orchestration. They simulate a real-world scenario where a \"parent\" workflow in a central account invokes a \"child\" workflow (analyzing product reviews with Amazon Bedrock) in a different account. The demo reveals a common pitfall: the parent workflow remains stuck in a \"Running\" state even after the child workflow successfully completes. Diego explains that unlike same-account invocations which use EventBridge for instant notification, cross-account patterns rely on the parent actively polling the child's status. Using CloudTrail, they identify the root cause—an AccessDenied error on the DescribeExecution API call—and demonstrate the fix by updating the child account's IAM role to explicitly trust the parent and allow stepfunctions:DescribeExecution and stepfunctions:StopExecution. This deeper understanding of the polling mechanism versus event-driven architecture is crucial for debugging multi-account serverless architectures."
keywords: AWS Step Functions, Distributed Map, Serverless Observability, Cross-Account Architectures, CloudWatch Metrics
---
