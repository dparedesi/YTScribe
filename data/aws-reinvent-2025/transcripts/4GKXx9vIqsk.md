---
video_id: 4GKXx9vIqsk
video_url: https://www.youtube.com/watch?v=4GKXx9vIqsk
is_generated: False
is_translatable: True
summary: "DynamoDB engineers Amrit and Craig present how DynamoDB makes architectural decisions based on four core tenets: Security (non-negotiable, all data encrypted at rest), Durability (non-negotiable, data written to two availability zones before commit), Availability (non-negotiable, regional service surviving complete availability zone failures), and Predictable Low Latency (DynamoDB's differentiating feature, guaranteeing single-digit millisecond latency at any scale). The presentation emphasizes that large organizations need distributed decision-making through shared tenets rather than centralized control, using DynamoDB as an example where teams understand these principles to make decisions independently. The service operates at massive scale with hundreds of tables pushing 500,000 requests per second daily, and customers like Amazon Stores peaking at 151 million requests per second on Prime Day, requiring architectural choices that handle distributed state management complexity where stateless systems are challenging but stateful systems add another level of complexity. The presentation explores distributed state fundamentals, explaining why single instances work for simple cases but scaling requires separating state, introducing failure handling, coordination complexity, and the progression from primary-secondary models (facing split-brain problems with even numbers) to three-node clusters (minimum for surviving single failures) to larger clusters, ultimately concluding that groups of three nodes provide optimal balance between availability, cost, and repair speed, with the insight that effort spent on additional nodes might be better invested in faster node repair. The architecture uses groups of three nodes across thousands of storage nodes, requiring metadata systems to route requests to correct data locations, introducing the challenge of metadata lookups that could become bottlenecks if scaled linearly with request volume, leading to caching solutions that must handle eventual consistency, cache invalidation, and the risk of large request router fleets overwhelming small metadata fleets when caches go cold. DynamoDB implements a two-tier caching system with MEMDS as one tier and request routers as another, both eventually consistent, using versioned data where storage nodes are the authoritative source reporting partition information with version numbers, enabling cache misses to be detected when version mismatches occur (like version 20 pointing to old location when storage node reports version 21), with immediate redirection to correct locations ensuring requests are served even with eventually consistent caches. The system implements hedging (making two requests to two MEMDS servers on cache misses, taking the first response to improve latency predictability), constant work (sending two background requests to MEMDS servers even on cache hits to prepare for potential cache cold scenarios), and version-based conflict resolution, with the key insight that caching is extremely difficult and requires understanding eventual consistency, building systems with versioned data, and accepting that synchronous updates across large node counts are not scalable. Request routing architecture includes DNS load balancers distributing to network load balancers, which route to request router fleets that perform SigV4 authentication and authorization, metadata lookups to determine storage node locations, and request forwarding, with all components running as EC2 instances across availability zones. Availability zone optimization uses split-horizon DNS to route clients to load balancers in their same availability zone, reducing network latency since inter-AZ network distance significantly exceeds intra-AZ distance, and this latency reduction is meaningful because server-side processing time is similar in magnitude to network distance, making distance reduction one of the biggest improvements possible for predictable low latency. The system maintains one-third of traffic and one-third of leaders in each availability zone for even distribution, but prioritizes availability over latency when availability zones fail, accepting 50% traffic increases in remaining zones and maintaining idle capacity for such scenarios, with complex DNS management ensuring no more than one-third of traffic goes to any zone even with traffic skews, though law of large numbers and internet traffic routing help balance loads in practice. Storage node routing uses local availability zone routing for eventually consistent reads (sending traffic to replicas in the same AZ) while strongly consistent reads must go to elected leaders, with monitoring to detect when local routing would overload single nodes and automatically falling back to random routing to prioritize availability, though in practice high-traffic tables with clients distributed across AZs rarely trigger this fallback. Partition metadata management faces the challenge that synchronous updates would require handling hundreds of thousands of partition movements and splits per second, making it unscalable, while customer requests numbering in billions per second would require metadata systems scaling to front-door traffic levels, leading to the two-tier eventually consistent caching solution with versioning, hedging, and constant work patterns. The partition location system must operate in tens of microseconds to route hundreds of millions of requests per second, handling constant changes from table creation, deletion, traffic scaling, partition splitting, and partition movement, requiring request routers to determine storage node locations despite storage nodes constantly shuffling partitions, changing key ranges, and updating leadership, solved through versioned eventually consistent metadata with authoritative storage nodes, two-tier caching, and conflict resolution mechanisms. DynamoDB uses primary key hashing to partition tables, computing hashes of primary keys, ordering data by hash values, and creating contiguous hash ranges that become partitions, with request routing computing item key hashes, finding which hash range the item falls within, and routing to that partition's storage location, all accomplished in tens of microseconds with monitoring to ensure this bound is maintained. Limits in DynamoDB including read/write capacity units (currently 1000 RCUs, 1000 WCUs, 3000 RCUs per partition), transaction size (25 items at launch, now 100 items with standard serializable isolation using two-phase commit for writes and double-read optimization for reads), item size (400KB limit to prevent impact on other items in shared partitions), and Global Secondary Indexes (5 at launch, now 20 with consistent predictable replication lag) all exist to guarantee predictable low latency on shared physical hardware infrastructure, with partition warm throughput feature helping customers understand actual traffic capacity by identifying which partition will throttle first rather than meaningless partition counts. Customer recommendations include using long-lived connections with right-sized connection pools, hedging requests (sending two requests for reads, ensuring idempotency for writes), building constant work into cache designs, understanding that caching is extremely difficult requiring eventual consistency acceptance, and recognizing that DynamoDB's limits exist to maintain predictable latency guarantees. The presentation concludes by emphasizing that every DynamoDB decision reflects the four tenets, with security requiring encryption at rest, durability requiring two-AZ writes before commit, availability requiring regional resilience, and predictable low latency driving all architectural choices, encouraging organizations to define their own tenets and use them to drive distributed decision-making rather than relying on centralized control."
keywords: "DynamoDB, Distributed Systems, Request Routing, Caching Strategies, Predictable Low Latency"
---
