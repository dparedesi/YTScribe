---
video_id: H6_YsMnFZRE
video_url: https://www.youtube.com/watch?v=H6_YsMnFZRE
is_generated: False
is_translatable: True
summary: This interactive code talk demonstrates building an MCP (Model Context Protocol) server for Amazon Aurora PostgreSQL, showing how to securely expose database data to LLMs through a live coding session using Quiro IDE. The presentation begins by establishing MCP as a universal framework for exposing backend data to LLMs, a year-old maturing specification with recent security enhancements, leveraging the AWS Aurora PostgreSQL MCP server as a foundation. The goal is building an entertainment hub application using Strangeworks framework that queries an IMDb dataset stored in Aurora PostgreSQL, with the MCP server providing two tools: get_movie_characters and get_actor_roles that pull data from the database, while Strangeworks uses Amazon Bedrock for the LLM. The session emphasizes the canned query pattern where deterministic queries are wrapped into tools rather than allowing LLMs to dynamically discover schemas and construct queries, which reduces agent loop cycles, latency, and token costs at scale. The development process starts with writing SQL queries first (the database-first approach), then wrapping them into MCP tools, beginning with a query to return all characters and actors who played them for a given movie title, using full-text search capabilities and handling multiple matches by selecting the most popular title through ratings table joins. A second query returns all roles and movie titles for a given actor, with both queries saved for later use in the MCP server implementation. The MCP server is built using Python with the MCP SDK, connecting to the database using psycopg3, loading connection information from environment variables, and implementing the two tools that execute the pre-written queries. The presentation highlights the importance of database comments for sharing schema knowledge with LLMs, using PostgreSQL's COMMENT command to describe columns, data types, nullability constraints, and usage instructions (like TSVECTOR for full-text search), enabling LLMs to understand database structure without requiring separate documentation. Security becomes a critical focus as opening databases to LLMs presents significant risks. The session demonstrates implementing Row Level Security (RLS) in PostgreSQL to deterministically control data access based on user entitlements, using environment variables set via set_config function to store user-specific access controls (e.g., title types a user can access), creating RLS policies that automatically append WHERE clauses to all queries, ensuring users only see data they're authorized to access regardless of what the LLM attempts to query. The implementation shows connecting as different users (table owner vs MCP user) to demonstrate RLS enforcement, with policies configured to return no data by default if entitlements aren't set, then explicitly allowing access when proper configuration is applied. The MCP server is modified to accept command-line parameters for title types, load user entitlements from a users table during login, and pass these entitlements to the database connection using set_config, ensuring that even if LLMs attempt to bypass restrictions, the database-level security policies enforce access controls deterministically. The presentation addresses practical considerations including the nondeterministic nature of LLMs (they may answer from training data rather than using tools, as demonstrated when asking about Star Wars), the importance of forcing tool usage for obscure data not in training sets, and the challenges of passing context between different memory contexts in development environments. Key security principles emphasized include never trusting LLMs to enforce security policies, leveraging database-native security features like RLS for deterministic enforcement, defaulting to no data access rather than exposing wrong data, and using the database as the source of truth for access control rather than application-level checks. The session concludes with a GitHub repository containing all code, database setup scripts, and documentation for recreating the demonstration, acknowledging that production deployments require careful consideration of token costs, latency implications of multiple agent loops, and the trade-offs between deterministic RAG approaches (pre-computed vectors) versus flexible SQL-based approaches (real-time queries with higher latency and cost). The presentation serves as both a technical tutorial and a security best practices guide for safely exposing databases to LLM applications through MCP servers.
keywords: MCP server, Amazon Aurora PostgreSQL, database security, Row Level Security, LLM integration
---
