---
video_id: sCHlFiEEY6E
video_url: https://www.youtube.com/watch?v=sCHlFiEEY6E
is_generated: False
is_translatable: True
summary: "Mark from the Aurora DSQL team presents deep dive into connection management and architecture bridging traditional relational database features with distributed systems eliminating single points of failure through PostgreSQL-based fully serverless database with pay-per-use pricing scaling to zero with 5-second cluster creation requiring no capacity planning. Architecture includes query processor receiving PostgreSQL wire protocol, journal as distributed transaction log writing transactions to minimum 2 availability zones with multi-region replication, and storage service maintaining state enabling query execution with DML operations residing in memory until commit. Strong consistency achieved through time-based synchronization using EC2 Time Sync Service GPS satellite clocks corrected by AWS Clock Bound Service producing linearizable timestamps guaranteeing latest data visibility through multi-version concurrency control, with active-active architecture allowing connections across different availability zones or regions coordinating through adjudicator implementing optimistic concurrency control. Automatic scaling implements continuous backups creating storage clones, splitting for manageable size, and journal layer sharding supporting cross-shard commits. Traditional PostgreSQL requires Unix fork creating dedicated process per connection consuming resources leading to client-side connection pools but creating peak-to-average problem requiring 10x pool size with 300 connections for multi-replica deployments necessitating large instances with 5000 connection limits prompting PGBouncer or RDS Proxy adding complexity, while single writer creates single point of failure requiring 30-second minimum downtime. DSQL shared endpoint uses single endpoint across clusters leveraging TLS Server Name Indication identifying clusters through randomly generated identifiers in Route 53 alias records, with Firecracker micro VMs providing security isolation through warm pools of hundreds of VMs per machine using Snapstart creating seed VM writing memory pages to persistent file launching identical clones sharing memory-mapped pointers paying only for unique differences. Relay service built in Rust using S2N library provides TLS parsing and authentication token validation using SigV4 pre-signed URL mechanism enabling nanosecond token generation integrating with IAM data plane enforcing resource-based policies, consulting placement service tracking fleet status then moving TLS session into sandbox packaging encryption keys securely zeroing relay memory. Per-cluster pooling enables instant connection reuse automatically pulling additional QPs when busy eliminating PGBouncer need with placement service maintaining in-memory information supporting default 10,000 connection limit doubling RDS instance capacity. Connection age limited to one hour encouraging client-side pooling enabling retry during failures, with AZ-local routing through same-zone infrastructure providing lowest latency with cross-AZ fallback potentially faster than single-writer endpoints since DSQL has no primary. Activity-based pricing measures compute DPUs for per-second QP time, read DPUs for bytes visited, write DPUs for commit payload bytes, and storage in gigabyte-hours charging only single copy with zero compute cost when idle, while security through micro VM isolation and end-to-end encryption encourages public endpoint usage with short-lived tokens, scalability through pay-per-activity billing, simplicity eliminating patching and maintenance, and performance through AZ-local routing and generous throttling handling mass reconnect storms."
keywords: Aurora DSQL, distributed database, PostgreSQL, serverless architecture, connection management
---
