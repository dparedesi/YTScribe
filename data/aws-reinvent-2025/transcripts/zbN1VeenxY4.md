---
video_id: zbN1VeenxY4
video_url: https://www.youtube.com/watch?v=zbN1VeenxY4
is_generated: False
is_translatable: True
summary: This session provides a deep technical dive into KIP-932, which extends Apache Kafka beyond traditional streaming to support queue-like messaging semantics, addressing fundamental limitations in Kafka's consumer group model that restrict parallelism to partition count and create head-of-line blocking issues where slow message processing stalls entire partitions. Shubham Raksheed and Masido Saim, senior streaming solution architects at AWS, introduce the session by explaining Apache Kafka's core architecture as a distributed, partitioned, replicated commit log service functioning as a fault-tolerant, ordered journal where producers write events and consumers read from topics implemented as partitions, with each partition being an immutable append-only log addressed by sequential offsets, where producers write events to partitions based on keys ensuring same-key events go to same partition, and consumer groups form to read data sequentially from partitions with each partition consumed by exactly one consumer while one consumer can handle multiple partitions. The motivation for queue semantics arises from real-world limitations where ingress traffic spikes during business hours, campaign launches, or seasonal events impact consumer processing ability, causing consumer lag to rise, while typical Kafka deployments have multiple applications consuming the same data, making outbound consumption throughput often higher than ingress, requiring the ability to scale consumption along with increased throughput, but consumer parallelism is bound to partition count, meaning scaling consumers requires increasing partitions, which becomes problematic when partitioning for unknown peak loads leads to over-partitioning with operational overhead and cost, and repartitioning later disrupts consistent hashing, breaking partition key ordering guarantees, forcing teams to create new topics and migrate applications. Head-of-line blocking occurs because Kafka guarantees ordering within partitions, so if a single message is slow to process due to downstream API calls, validation checks, or malformed data, all subsequent records are blocked even if they could be processed instantly, with workarounds including increasing partitions to distribute data more widely, asynchronous or parallel processing requiring complex state management, or skipping problematic messages with additional consumer code, all adding complexity. Kafka's native model doesn't behave like traditional queues optimized for parallel consumption where ordering is less important than processing speed, with parallelism bound to partition-to-consumer mapping where Kafka restricts each partition to a single consumer in a consumer group, making queue-like patterns requiring per-message acknowledgement, retries, solving head-of-line blocking, or dynamically scaling workers difficult without custom frameworks that increase complexity. KIP-932 introduces Share Groups, a new consumer group type allowing multiple consumers to read from the same partition concurrently, enabling higher parallelism than traditional consumer groups without adding partitions, particularly useful when consumer count exceeds partition count, allowing workload distribution across available consumers without over-partitioning topics, with share groups and consumer groups able to coexist on the same Kafka broker using different group IDs in the same namespace. Record delivery in share groups uses two offset types: share partition start offset referring to the starting offset of records eligible for consumption, and share partition end offset referring to the last offset of records eligible for consumption, with records between start and end offsets called in-flight records that consumers fetch and acknowledge once processed, with failed messages being retried by other consumers, while the share partition leader controls concurrency using group.share.partition.max.record.locks configuration to manage the number of in-flight records and prevent overload. Records progress through states including archived state where data is no longer eligible for consumption, acquired state where data is locked by a consumer for processing, available state where data is available for consumption and assignment, and acknowledged state where data is successfully processed, with delivery counts tracking how many times records have been delivered, enabling retry logic and archival of records exceeding delivery limits. Ordering semantics in share groups differ from traditional consumer groups, where records may arrive out of order across multiple consumer batches, especially when retried, with ordering maintained within each consumer batch but not across batches, making the model designed for scalability rather than strict sequencing, which is acceptable for queue-style workloads where processing speed matters more than global ordering. Offset management uses a sliding window where start offset advances when records are acknowledged and end offset upper bound is determined by max record locks configuration, with start offset defaulting to latest but configurable to earliest for reprocessing, while log retention and compaction work similarly to consumer groups, with share partition start offset bounded by log start offset managed by retention policies, and compaction able to run on topics consumed by share groups with potential offset gaps when records are cleaned during consumption, which are holes in numbering rather than data loss. Bad record handling includes three scenarios: records that fail during processing can be explicitly acknowledged and marked as released for transient errors or rejected for permanent errors, records that fail deserialization throw RecordDeserializationException with Kafka automatically releasing them, and records failing cyclic redundancy checks throw CorruptRecordException with Kafka rejecting entire batches to prevent other consumers from picking up corrupted data, providing built-in mechanisms to avoid stalling on problematic records while maintaining throughput and resilience. Use cases for queue semantics include task worker processing patterns for discrete work items like image transformation, ML inference jobs, or data enrichment where messages represent tasks that can be distributed across worker pools with fault tolerance, microservice job queues for asynchronous workloads like sending emails, generating PDFs, or billing that rely on parallelism and retries without strict ordering requirements, and asynchronous request execution for operations that should run asynchronously to keep applications responsive, like fraud detection during checkout that emits requests to Kafka topics for ML model processing without blocking user interfaces. The internal implementation uses a new internal topic called __share_group_state__ for share group state management, a highly partitioned topic with 50 partitions by default using delete cleanup policy, not compacted, with share coordinators periodically deleting records to optimize space, where share coordinators become leaders of share group partitions, scan share group state, build in-memory state, and start serving requests, with share partition leaders residing on the same broker as topic partition leaders, managing share group state, interacting with topic partitions, fetching records from local replicas, and tracking message states. Delivery semantics involve consumers pulling records, brokers marking records as locked and in-flight, consumers processing and acknowledging records with brokers marking them as acknowledged, with timeout scenarios releasing locks and making records available again, and problematic messages tracked by delivery count with limits determining whether to retry or archive, balancing reliability and resiliency to guarantee at least-once delivery regardless of failures. Lock renewal semantics apply acquisition locks when consumers fetch records, preventing other consumers from reading the same message, with locks held for default 30 seconds during which consumers must process and acknowledge, with successful acknowledgement marking records as acknowledged, explicit release making records available for other consumers, and automatic lock release on timeout returning records to available state for re-delivery. Consumer failure handling involves brokers being unaware of consumer crashes, with locks remaining active until timeout, group coordinators removing members when heartbeats stop, brokers automatically releasing locks based on lock duration settings, and consumers able to rejoin and resume processing after transient failures, while broker failure handling involves new leader election for share coordinators, state recovery from highly replicated internal topics, building in-memory state to understand record states and consumer assignments, with minor pauses during leader election but guaranteed no data loss. The API implementation uses standard Kafka consumer patterns with group ID configuration for share groups, poll methods with duration parameters, explicit acknowledgement methods for accept, release, or reject actions, and commit methods for batch processing, with security and ACL management remaining the same as traditional Kafka consumers, requiring read and write access to the share group state topic for brokers, and new API operations requiring appropriate permissions as documented. Additional metrics are available for brokers including group count metrics, share group count metrics, rebalancing rate and count, partition load time from internal topics, and write latency to internal topics, while client metrics include heartbeat rate, last poll seconds, record fetch rate, fetch size, fetch latency, and heartbeat response time for monitoring consumer health, throughput, and latency. Current limitations include the feature being in preview in Kafka 4.1 and not recommended for production, lack of dead letter queue support, inability to monitor share group lag, and unsuitability for workloads requiring exactly-once processing or strict ordering guarantees, with roadmap items targeting production readiness in Kafka 4.2, dead letter queue support via KIP-1191, strict record feed via KIP-1206 to avoid overloading slow consumers, configurable acquisition lock extension via KIP-1222, share group lag monitoring via KIP-1226, and additional group-level settings via KIP-1240 for finer control over delivery limits and lock times without cluster-wide configuration changes. Key takeaways emphasize that KIP-932 brings queue capabilities to Kafka without over-partitioning, built on Kafka's scalable backbone, breaking one-partition-per-consumer behavior to enable parallel consumption for truly parallel workloads, expanding rather than replacing regular Kafka consumers, with Kafka scaling no longer tightly bound to partition count, allowing more consumers than partitions and shifting capacity planning focus to compute capacity on brokers and clients rather than topic over-provisioning, making peak load handling much easier from the consumption side without partition assignment complexity.
keywords: Apache Kafka, KIP-932, queue semantics, share groups, distributed systems
---
