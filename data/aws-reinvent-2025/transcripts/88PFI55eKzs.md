---
video_id: 88PFI55eKzs
video_url: https://www.youtube.com/watch?v=88PFI55eKzs
is_generated: False
is_translatable: True
summary: This deep dive session explores Amazon ECS managed instances and blue-green deployment capabilities, addressing how AWS has evolved container orchestration to reduce undifferentiated work while maintaining flexibility. The presentation begins by establishing ECS as a tier-one service launching over 3 billion tasks weekly across all regions, with more than 65% of new AWS customers starting with ECS due to its simplicity and alignment with AWS APIs. The session introduces ECS managed instances, launched in September, as a new compute engine that bridges the gap between ECS on EC2 (high control, high operational burden) and ECS on Fargate (simplicity, reduced control), delivering EC2 flexibility with a Fargate-like experience. The shared responsibility model is central to understanding this continuum: with ECS on EC2, customers manage auto-scaling, placement, launch templates, and instance types, while Fargate offloads most infrastructure management. ECS managed instances takes on responsibilities like compute auto-scaling, distribution and placement, launch template creation, and EC2 instance patching, while still providing flexibility for compute selection through attribute-based instance selection. The capacity provider architecture enables this, with the new ECS Managed Instances Capacity Provider sitting between managed ASG capacity providers and Fargate, using spread placement by default across availability zones for maximum availability. Instance selection uses task definition configurations (memory reservation, CPU ratios, task-level memory and CPU) to determine appropriate EC2 instances, with a preference for larger instances to enable multi-tenancy, image caching benefits, and workload elasticity for bursty applications like Java with JIT compilation. The scheduling algorithm prioritizes placing tasks on existing compute first, then uses spread placement for availability, followed by bin packing for cost optimization, using first-fit decreasing to select the largest suitable instance. Host replacement workflows handle maintenance events, patching requirements, auto-scaling changes, and cost optimization, following a golden rule of starting tasks before stopping them to maintain minimum healthy requirements. The patching workflow ensures all capacity is patched within 30 days, provisioning new BottleRocket-based instances on day zero, beginning patch opportunities on day 14 while honoring EC2 maintenance windows, and becoming more aggressive at day 21 to meet compliance commitments. The second major topic covers ECS deployment capabilities revamped in November, moving away from CodeDeploy dependency to native ECS deployment support with three strategies: blue-green deployments for fast traffic switching with 100% capacity provisioning and instant rollback capability, linear deployments for gradual traffic migration with configurable percentages and bake times between steps, and canary deployments combining initial small traffic percentage with subsequent full cutover. All deployment strategies follow six stages: preparation (infrastructure setup without resource provisioning), deployment (scale-up of new version to 100% capacity), testing (validation with test traffic injection), traffic shift (gradual or instant migration), monitoring (bake time with performance validation), and completion (cleanup of old resources). Deployment lifecycle hooks enable Lambda-based validation at each stage, allowing custom verification of container image SHAs, target group validation, and performance checks, with automatic rollback on failure. The session emphasizes that ECS implements best practices by design, including start-before-stop patterns, continual rebalancing as default behavior, idle host detection and optimization, and deployment patterns used internally at Amazon scale, with recommendations that most customers start with Fargate, use managed instances when needing larger task sizes or specialized compute like GPUs, and reserve ECS on EC2 for customers requiring custom AMIs or kernel tuning.
keywords: Amazon ECS, managed instances, blue-green deployments, container orchestration, infrastructure automation
---
