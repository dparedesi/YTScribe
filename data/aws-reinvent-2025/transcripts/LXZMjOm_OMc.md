---
video_id: LXZMjOm_OMc
video_url: https://www.youtube.com/watch?v=LXZMjOm_OMc
is_generated: False
is_translatable: True
summary: |
  Shri and Frédéric recapped 15 years of Amazon Linux evolution (AL1 in 2010, AL2 in 2017, AL2023 in 2023) and detailed the past year’s improvements to security, stability, and compliance. They reminded attendees why Amazon Linux remains the most-used OS on AWS: it is optimized for AWS services out of the box, ships frequent security fixes without licensing fees, eases operations with deep service integrations, and is ready for regulated workloads. Security work is continuous; the team evaluated tens of thousands of CVEs and shipped roughly 1,500 fixes for AL2023 this year alone, focusing on both timely patches and reducing false positives so customers are not forced into risky, unnecessary updates. Severities skew toward medium and important issues, with only a handful of criticals, underscoring the need for ongoing vigilance.
  Kernel lifecycle management was a major theme. Because the upstream LTS kernel cadence no longer aligns to OS lifetimes, customers should plan for at least one kernel upgrade during an AL2023 lifecycle. To make that painless, new SSM parameters let you pin or automatically track the “default” kernel for AL2023; if you opt into the default tag, future kernel releases (e.g., moving from 6.1 to 6.12 and beyond) are adopted transparently while predictable updates and rollback controls remain available. Explicit kernel-tagged parameters persist for customers with strict performance or compatibility requirements.
  Compliance milestones are now in place. AL2023’s cryptographic modules (OpenSSL, kernel crypto, NSS, GnuTLS, Libgcrypt) achieved final FIPS 140 validation, enabling default AMIs to run in FIPS-mandated environments. FIPS mode can be enforced at boot or per application, and a separate FIPS provider package decouples validated modules from the base OpenSSL version so revalidations can track updates. A DISA STIG for AL2023 was published in September; EC2 Image Builder and Systems Manager now offer managed documents to apply the STIG automatically during image builds or to running instances, sparing teams from manual hardening. They also flagged the approaching end-of-support for Amazon Linux 2 and urged customers to migrate to AL2023 using the new tooling and predictable update channels.
  Performance and usability saw notable gains. Upgrading to OpenSSL 3.2 (from 3.0) closed the TLS throughput gaps customers observed when moving off AL2’s OpenSSL 1.0.2; internal and customer benchmarks show 2–3x real-world TLS session improvements and up to 15x gains in synthetic tests. The FIPS provider remains installable alongside newer OpenSSL versions to preserve compliance. A new “graphical desktop” option responds to customer demand for GUI workflows (testing graphical apps, operator consoles), even though the distro is cloud-first. Finally, they reiterated Amazon Linux’s predictable quarterly releases, kernel-tag flexibility, and automation hooks as the path to keep systems secure and compliant without sacrificing stability.
keywords: Amazon Linux 2023, CVE management, kernel lifecycle, FIPS compliance, OpenSSL performance, DISA STIG
---
