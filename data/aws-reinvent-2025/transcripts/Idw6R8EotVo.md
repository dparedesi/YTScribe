---
video_id: Idw6R8EotVo
video_url: https://www.youtube.com/watch?v=Idw6R8EotVo
is_generated: False
is_translatable: True
summary: This session addresses the challenge of simplifying data lake access control by introducing IAM Identity Center's trusted identity propagation feature, which enables administrators to assign permissions based on human identities rather than IAM roles, fundamentally changing how access is managed for data lake workloads. The presentation begins by establishing the data lake architecture context where organizations ingest data from multiple sources using AWS Glue for connection and cataloging, transform data through cleansing and augmentation processes, and store processed data in services like S3 or Redshift for consumption by applications such as AI chatbots powered by Athena queries enriched with LLMs. Traditional IAM role-based access control creates significant challenges including the need to create separate roles for each persona like sales managers, marketing teams, and data science teams, resulting in role proliferation that becomes difficult to manage as access patterns grow, complicated policies that can easily reach size limits, high risk of overly permissive roles or unused permissions over time, and difficulty tracking individual user actions when multiple users assume the same role, making it challenging to audit specific user activities in services like Athena or Redshift. Trusted identity propagation solves these challenges by providing a unified access control model based on global identity, allowing administrators to assign permissions directly to end users or groups rather than modeling access in IAM policies, creating a single universal identity recognized across the AWS ecosystem with unique user IDs that are understood by all integrated applications. The implementation requires IAM Identity Center integration with identity providers like Okta, Entra, or Active Directory to bring identities into AWS, registration of AWS managed applications like QuickSight, Amazon Redshift, or Q with Identity Center, and assignment of access permissions to users or groups within those applications based on their Identity Center identity rather than assumed roles. The technical flow involves users authenticating with Identity Center, applications querying Identity Center's identity store to retrieve user information and group memberships, applications using service roles enhanced with user identity context to make downstream API calls, and each service call propagating the user's identity context through the entire request chain from entry point applications through Athena, Lake Formation, and S3. The key innovation is that service roles remain for AWS services to communicate with each other, but these roles are enhanced with user identity context rather than requiring end users to assume roles themselves, eliminating the one-to-many relationship where multiple users would assume the same role and making it impossible to distinguish individual user actions in logs. CloudTrail logging captures this identity propagation through an "on behalf of" field in every API call log entry, showing both the service making the call and the user identity on whose behalf the call is made, with user IDs that are unique across AWS organizations and include both the directory user ID and identity store ARN for precise identification. This centralized identity approach enables granular user-specific permissions without role proliferation, comprehensive audit trails showing exactly which user performed which actions across all services, simplified policy management focusing on service roles with minimum permissions rather than user role management, and identity-aware paths through downstream services that work not only with AWS managed applications but also custom applications like employee portals that can integrate with Identity Center. The presentation emphasizes that trusted identity propagation creates a unified access control model where the same user identity flows from identity providers through Identity Center to all AWS services, enabling administrators to grant access to schemas, tables, or data resources directly to users or groups in Identity Center, with that access automatically recognized by integrated services like Redshift with Lake Formation, creating a seamless and auditable access control experience that eliminates the complexity and security risks associated with traditional IAM role-based access patterns for data lake workloads.
keywords: IAM Identity Center, trusted identity propagation, data lake access, identity management, access control
---
