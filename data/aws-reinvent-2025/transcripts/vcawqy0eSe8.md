---
video_id: vcawqy0eSe8
video_url: https://www.youtube.com/watch?v=vcawqy0eSe8
is_generated: False
is_translatable: True
summary: This code talk demonstrates building an AI agent application that bridges structured and unstructured data using AWS services, addressing the enterprise challenge where knowledge exists in both relational databases (predominant storage mechanism for 15-20 years of enterprise systems) and unstructured data sources (generated over the past 10-15 years in various forms), with the solution using AI agents to intelligently navigate across these data silos. The architecture consists of a Streamlit UI application that accepts user questions and routes them to appropriate data sources, with unstructured data accessed through Amazon Bedrock Knowledge Bases (containing campaign-related documents in S3 converted to vectors for LLM understanding) and structured data accessed through Amazon Aurora PostgreSQL database (containing campaign information, donation data, and membership records), with the agent automatically determining which data source to query based on the question. The application is built using Strands Agents SDK, a Python-based framework supporting single and multi-agent systems with MCP (Model Context Protocol) and A2A (Agent-to-Agent) protocol support, where agents receive prompts, utilize built-in tools, and provide services based on tool capabilities, with the implementation using Claude (via Bedrock) as the primary LLM for agent communication and decision-making, while Nova model is used within custom tools for specific tasks like SQL generation, demonstrating the flexibility to use multiple models for different purposes. The unstructured data access is implemented using Strands' out-of-the-box retrieve tool that queries Bedrock Knowledge Bases without requiring custom code, requiring only the knowledge base ID and region configured through environment variables, with Bedrock Knowledge Bases handling the RAG infrastructure by converting documents (PDF, Word, etc.) into vectors using embedding models (Titan or Cohere), storing them in vector databases, and managing chunking strategies automatically. The structured data access is implemented through a custom tool called query_SQL_DB that takes user prompts, determines if questions can be answered through relational database data, converts natural language into appropriate SQL queries, and retrieves data, built as a Python function annotated with Strands' tool decorator to integrate with the agent framework, containing a supporting DBStructureDataAgent class that handles database schema retrieval, system prompt generation, SQL query generation, SQL extraction, and SQL execution. The custom tool implementation includes get_table_schema function querying PostgreSQL system tables to retrieve complete database schema (tables, columns, data types) in JSON format, generate_system_prompt function using Nova model to create intelligent prompts embedding database schema information that will be sent to Claude for query generation, generate_SQL function taking user prompts and schema-embedded system prompts to dynamically generate valid SQL queries with delimiter markers for extraction, extract_SQL function pulling SQL statements from LLM output between delimiters, and execute_SQL function executing queries against Aurora PostgreSQL using credentials stored in AWS Secrets Manager (following best practices of never hardcoding credentials), with the tool using Aurora ARN, Secrets Manager ARN, database name, and model configuration as constructor parameters. The agent's intelligence comes from Claude model making decisions about which tool to use (retrieve tool for unstructured data or query_SQL_DB tool for structured data) based on user questions, with system prompts playing a crucial role in defining agent personality and behavior, especially as complexity increases with multiple knowledge bases, multiple databases, data transformations, and data dictionaries, where more information in system prompts enables more intelligent tool selection. The application demonstrates semantic understanding where LLM can map user terminology (like "event") to database schema terminology (like "campaign") even when exact matches don't exist, with recommendations for production deployments including using memory tools (like Agent Core memory) to cache database schemas and data dictionaries that don't change frequently, enhancing system prompts with data dictionary information for cryptic column names, and deploying to AWS infrastructure options including Lambda, ECS, EKS, or Agent Core runtime for agentic applications, with the code designed as a learning example rather than production-ready, using hardcoded IDs and simplified error handling for demonstration purposes.
keywords: AI Agents, Structured Data, Unstructured Data, Bedrock Knowledge Bases, Aurora Database, Strands Agents SDK
---
