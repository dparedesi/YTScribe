---
video_id: i_lLnV6NgPg
video_url: https://www.youtube.com/watch?v=i_lLnV6NgPg
is_generated: False
is_translatable: True
summary: This session addresses the critical challenge of optimizing agentic AI applications for cost and latency through semantic caching with Amazon ElastiCache, presented by Sanjeet and Alan from AWS, focusing on the evolution of agentic AI from experimentation to production deployment where the mindset has shifted from does it work to does it work at scale, with organizations now thinking about security, governance, latency, and cost containment as 79% of companies have deployed agentic AI applications in production. The presentation identifies three barriers to moving from demo to deployment: scale complexity as applications include more agents, tools, and steps requiring more LLM calls, API calls, and tool calls; speed issues with compounding latency from assembling context, retrieving memories, planning agent and tool invocations, executing those invocations, and re-ranking responses, with applications cycling repeatedly for single transactions creating boundless latency and costs without proper guardrails; and cost challenges where adding more agents and steps increases intelligence but drives up costs and latency, requiring optimization techniques like prompt caching, cheaper models, model distillation, fine-tuning, better routing, and semantic caching to maintain intelligence while controlling costs. The session demonstrates the problem through a multi-agent framework example where a user asks to find Italian restaurants near the Bellagio with vegetarian options at specific times, requiring a planner agent to decompose the question, a search agent to find restaurants, a review agent to check vegetarian options, and an availability agent to check times, with each agent making multiple LLM calls, API calls, and tool invocations, creating compounded latency and cost, but when a second user asks a semantically similar question, the same expensive process repeats unnecessarily when cached responses could be reused. The solution is semantic caching using ElastiCache for Valky, an open-source community replacement for Redis, with vector search capabilities that offer the lowest vector search latency at highest recall and throughput levels, where semantic caching differs from traditional SQL-based caching by indexing cache entries by semantics rather than exact text, using embedding models to convert queries into vectors representing semantic meaning, then searching for vectors within a similarity threshold to find cached answers, with embedding models being 750 times faster and cheaper than large language models, making the caching overhead minimal. Alan explains the technical implementation, detailing how semantics are converted to vectors using embedding models available on Amazon Bedrock, including text embedding models and multimodal models for sound or image-based queries, with the ability to use vendor models or train custom domain-specific embedding models, and how semantic search becomes vector search where similar queries cluster together in vector space with small distances indicating semantic similarity, requiring the establishment of similarity thresholds to determine when cached answers are acceptable. The presentation addresses the scalability challenge of exact vector search, which has no known algorithm better than brute force after 75 years, introducing approximate nearest neighbor (ANN) algorithms that trade exactness for performance, with recall measuring what percentage of time the algorithm gets the right answer, and explaining hierarchical navigable small worlds (HNSW) as a graph-based algorithm that builds distance-based linkages between vectors, using a skip list-inspired pyramid structure to enable logarithmic search time, achieving 95-99% recall with answers in hundreds of microseconds or single-digit milliseconds even with tens to hundreds of millions of entries, with the critical insight that high recall at every step matters because recall multiplies across agent chains, where 90% recall per agent results in only 73% overall accuracy while 99% recall per agent achieves 97% overall accuracy. The cost analysis demonstrates that semantic caching breaks even at just 2.5% hit rate because embedding models cost 1/750th of LLM inference, with cost reduction almost exactly following hit rate, making it an extremely low-barrier optimization technique that yields significant benefits, and the implementation details cover how ElastiCache for Valky, as a key-value store, supports vector operations through a search module that maps keys to rows in a relational-like structure, where each value contains a vector component and a reference to the answer stored externally in DynamoDB, S3, or file systems, with indexes defined using JSON or hash formats, supporting multiple logical caches in a single instance through key space prefixes, using HNSW algorithm with configurable vector dimensions matching embedding model sizes, and automatic indexing when data is written through standard Valky commands, with immediate queryability unlike other vector stores that require minutes or hours for indexing. The session provides best practices for implementing semantic caching in agentic AI applications, emphasizing that agents tuned to specific goals with different tools, prompting models, and underlying data require corresponding semantic cache configurations with appropriate embedding models, search algorithms, similarity thresholds, and TTLs (time-to-live) for key expiration, where different agents have different data volatility tolerances requiring specific expiration settings, with the two key knobs being TTLs that balance freshness versus hit rates and similarity thresholds that balance accuracy versus hit rates, where lower TTLs and higher similarity requirements mean lower hit rates but more freshness and accuracy, while higher TTLs and lower similarity requirements mean higher hit rates with more cost and latency savings, with ElastiCache for Valky maintaining high accuracy through 99% recall even with lower similarity thresholds. The presentation concludes with guidance on caching based on application use cases, warning that agents accessing real-time or compliance data require caution in reusing cached responses due to fast-changing nature and compliance risks, while slow-changing data like restaurant menus are safe to cache, and emphasizing that optimization is a continuous journey requiring techniques like semantic caching, smaller LLMs, model distillation, and better routing as applications scale with more tools, agents, and users, positioning semantic caching as a critical tool for building super fast, cost-effective, and accurate agentic AI applications that can break even at 2.5% hit rates while maintaining high quality through superior recall algorithms.
keywords: semantic caching, agentic AI optimization, ElastiCache Valky, vector search, HNSW algorithm, approximate nearest neighbor, cost optimization, latency reduction
---
