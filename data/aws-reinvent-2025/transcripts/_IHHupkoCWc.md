---
video_id: _IHHupkoCWc
video_url: https://www.youtube.com/watch?v=_IHHupkoCWc
is_generated: False
is_translatable: True
summary: "This session introduces Amazon S3 Tables, a purpose-built AWS service launched at re:Invent 2024 that simplifies security, improves performance, and reduces maintenance complexity for managing tabular data at scale in data lakes. Brettie from AWS explains that S3 Tables addresses three primary problems: security simplification by allowing customers to write security policies against table resources rather than individual objects, which doesn't scale well with millions of objects, automatic compaction that reduces the number of objects requiring queries by consolidating data files automatically without customers needing to run custom Spark jobs or maintain compute resources, and automated table maintenance that cleans up unreferenced files and snapshots to reduce storage costs while preserving time travel and rollback capabilities. Venkatesh Mandallapa from Indeed then presents a comprehensive case study of Indeed's data lake migration to S3 Tables, describing Indeed as the world's number one job site with 635 million job seeker profiles, 33.3 million employers across 60+ countries, and 11,000 employees, with their Data Lake team operating a central data store processing massive scale data. Indeed's data lake architecture includes AWS Glue Data Catalog as the central catalog exposed to all customers across one AWS account, data stored in S3 Standard with a mix of Hive data in ORC format (68 petabytes) and Iceberg data in Parquet format (approximately 20 petabytes), accessed by multiple engines including Athena, Snowflake, Trino, and Spark, with staggering statistics including 87 petabytes total data lake size, 5,550 terabytes ingested daily, 15,000+ tables across different schemas, 6 ingestion patterns, and 170,000+ queries per day supporting analytical, ML, and operational workloads across streaming, batch, and ETL modes. Indeed's migration journey began with a goal to modernize their entire data lake to Iceberg format, initially planning to use standard S3 general-purpose buckets, but after Amazon released S3 Tables and the product matured over 6 months, they conducted a cost-benefit analysis and decided to migrate 100% of their data lake to S3 Tables, which also enabled them to reduce their 6 ingestion patterns to a single unified pattern, dramatically simplifying their codebase and maintenance. The challenges Indeed faced with Iceberg in general-purpose buckets included spending 2,000+ developer hours annually managing maintenance operations like compaction and deleting orphan files through custom Spark jobs running on Kubernetes, which became increasingly challenging to maintain, an average one-day onboarding time per customer team to get data into the data lake, S3 rate limiting issues causing severe incidents where data became unavailable due to different access patterns for frequently versus infrequently queried tables, and per-object tagging for access control across 600 million objects making security management, reclassification, and assessment extremely difficult, with reclassifying a single table from company-wide access to Glassdoor-only access requiring weeks of object tag updates. S3 Tables solved these problems by simplifying data governance through table-level resource policies enabling instant reclassification with a single policy update operation, providing approximately 10% annual AWS cost savings compared to general-purpose bucket Iceberg tables when factoring in storage, maintenance, compaction, and ingestion costs, reducing onboarding time from one day to minutes where producing teams can write to S3 table buckets via API calls and start ingesting data immediately, and returning developer months back to product development by eliminating maintenance operations. Indeed's migration strategy employs an incremental approach rather than big-bang, analyzing server access logs and query logs to categorize workloads into cohorts and batches across multiple phases: first migrating all Hive data to S3 Tables, then migrating Iceberg S3 Standard data to S3 Tables, and updating data producers, with further breakdown by workload types (Spark, Trino, Athena) and batch sizes at terabyte or petabyte scale. The dual-write pipeline strategy involves maintaining parallel pipelines where one writes to existing S3 Standard storage while another writes to S3 Tables, with both catalogs containing the same objects, followed by a cutover process linking the S3 Tables catalog to the default Glue catalog so consumers accessing the default catalog transparently read from S3 Tables backend, and final cleanup removing old S3 Standard data and the legacy pipeline. Key learnings from Indeed's migration include S3 Tables being tightly integrated with Lake Formation requiring identity and resource policy updates for organizations not using Lake Formation, query performance differences between Hive/ORC and Iceberg requiring workload validation, partition strategy changes needing compatibility verification, S3 Tables limits of 10,000 table buckets per region per account and 10,000 tables per bucket (100,000 total assets) requiring careful bucket planning for Indeed's projected growth from 15,000 to 20,000-25,000 tables, the need for custom backup and restore strategies leveraging unreferenced file features and policies, concurrent writer limits being an Iceberg challenge requiring writer count management per table, server access logging differences requiring pipeline updates to use CloudWatch and CloudTrail instead of S3 access logs, and Snowflake integration benefits through both Glue REST catalog and Iceberg REST catalog support enabling full data lake exposure in Snowflake. Indeed's migration progress shows 50 petabytes scheduled for migration with 2.5 petabytes in canary testing during Thanksgiving and December, always maintaining revert capability to ensure zero consumer impact, accepting dual-pipeline cost increases to prioritize consumer stability given global usage including policymakers and media, and being on track for a unified, modern, and cost-efficient data lake that meets all their requirements."
keywords: S3 Tables, Data Lake, Iceberg, Indeed, Data Migration, Table Maintenance, Compaction, Data Governance
---
