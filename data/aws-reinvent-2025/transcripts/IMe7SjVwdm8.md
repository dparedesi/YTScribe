---
video_id: IMe7SjVwdm8
video_url: https://www.youtube.com/watch?v=IMe7SjVwdm8
is_generated: False
is_translatable: True
summary: This session introduces AWS Certificate Manager's exportable public certificates feature, enabling customers to export private keys from public certificates for use on non-integrated services like EC2 instances, on-premise firewalls, containers, and multi-cloud deployments, addressing the challenge of certificate management where manual processes lead to errors, renewal gaps causing outages, and inconsistent installation across diverse environments. The feature allows customers to consolidate to a single certificate provider while maintaining the managed renewal experience for integrated services like Application Load Balancer, CloudFront, and API Gateway, with pricing set at $15 per FQDN for standard certificates (one-time fee upon export) and $149 for wildcard certificates, only charged when certificates are actually exported rather than when marked as exportable. The session addresses common certificate management challenges including manual installation processes requiring human intervention, diverse environments needing certificates (EC2, EKS, containers, on-premise systems, firewalls, other cloud providers), renewal gaps where expired certificates cause outages, and the need for automated renewal workflows. The solution architecture implements two workflows: on-demand certificate installation where an API Gateway receives certificate ARN and target EC2 instance tags, triggering a Step Functions workflow that exports the certificate from ACM with a dynamically generated passphrase stored in Secrets Manager, stores metadata in DynamoDB tracking where certificates are installed, and uses AWS Systems Manager automation documents to install certificates on Linux EC2 instances at scale based on tag-based targeting, with the solution supporting cross-account deployments through IAM role assumptions. The automated renewal workflow uses EventBridge rules listening for ACM certificate renewal events (triggered 60 days before expiry), Lambda functions that look up certificate installation locations from DynamoDB, and automatically re-executes the Step Functions workflow to export renewed certificates, update passphrases in Secrets Manager, and reinstall certificates on the same EC2 instances where they were previously installed, providing hands-off renewal similar to integrated services. Security considerations include private keys encrypted with KMS keys protected by FIPS 140-3 Level 3 validated HSMs while in ACM, exported private keys encrypted with passphrases, passphrases stored in Secrets Manager with tag-based access control using ABAC (Attribute-Based Access Control) ensuring only EC2 instances with matching tags can access passphrases to decrypt private keys, and IAM policies protecting API Gateway endpoints to prevent unauthorized certificate exports. The solution supports cross-account deployments where central accounts issue certificates and Systems Manager automation documents execute in target accounts, with passphrases stored in the same account as EC2 instances to avoid cross-account secret access, and IAM roles enabling Systems Manager to run commands across accounts. The implementation includes DynamoDB tables storing certificate metadata including ARN, expiry dates, installation locations, target accounts, and Secrets Manager ARNs for passphrase storage, enabling certificate inventory tracking and automated renewal lookups. The session demonstrates the solution working with sample code available on GitHub in AWS Samples repository, showing live execution of certificate export, passphrase generation and storage, Systems Manager automation execution, and certificate installation on EC2 instances, with the code being open source and extensible for custom applications like IIS servers, EKS clusters, or third-party cloud providers. Best practices emphasized include using DNS validation over email validation for no-touch renewal experiences, protecting exported private keys with strong passphrases stored securely, implementing tag-based access control for fine-grained permissions, automating renewals to handle upcoming CAB Forum changes reducing certificate lifetimes to 180 days in March and eventually 47 days by 2029, and using IAM condition keys to restrict certificate issuance based on domain names, certificate types, or wildcard usage. The session addresses questions about ACME protocol support (not currently available but under consideration), cross-account Route 53 zone integration challenges, service mesh and mTLS use cases (requiring private certificates from AWS Private CA rather than public certificates), and extensibility for ECS, Kubernetes, and other containerized workloads.
keywords: AWS Certificate Manager, exportable certificates, automated renewal, certificate management, Systems Manager
---
