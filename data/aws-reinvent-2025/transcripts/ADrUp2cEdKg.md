---
video_id: ADrUp2cEdKg
video_url: https://www.youtube.com/watch?v=ADrUp2cEdKg
is_generated: False
is_translatable: True
summary: "This advanced Level 400 technical session, presented by AWS Principal Solutions Architect Pallavi Nargund and Solutions Architect Vivek Mittal, dives deep into strategies for improving the accuracy of Retrieval Augmented Generation (RAG) architectures using Amazon Bedrock Knowledge Bases. The speakers acknowledge that while standard or \"naive\" RAG setups work well for simple demonstrations, they often struggle with complex enterprise use cases involving large volumes of unstructured data, proprietary terminology, and multi-faceted user queries. The session is code-heavy, using a Jupyter notebook demonstration centered around a fictitious financial company, \"Octank Financial,\" and its 10K report. They begin by establishing a baseline using Amazon Bedrock Knowledge Bases with OpenSearch Serverless as the vector store, utilizing the Anthropic Claude Haiku model for generation.

The core of the presentation explores three specific advanced patterns to handle scenarios where standard RAG fails. First, they demonstrate **Query Decomposition**. When a user asks a compound question—such as inquiring about both a specific building (\"Octank Tower\") and a \"whistleblower scandal\"—a standard retrieval might miss one component. By configuring an orchestration layer to decompose the query, the system breaks the request into distinct sub-queries, retrieves context for each independently, and synthesizes a complete answer. Second, they address the challenge of domain-specific acronyms using **Query Expansion**. When the system encounters an undefined term like \"DCM,\" standard retrieval fails. The speakers implement a \"Query Expansion Agent\" that utilizes a tool to look up terms in a DynamoDB glossary. The agent identifies \"DCM\" as \"Disclosure Committee Members,\" reformulates the query with the full term, and successfully retrieves the relevant names from the 10K document.

The most sophisticated pattern introduced is **Self-Corrective Agentic RAG**. This architecture employs a central orchestrator agent that dynamically evaluates the quality of both the retrieval and the generation steps. The workflow involves a \"context relevance check,\" where an LLM evaluates if the retrieved chunks actually answer the query. If they do not, the agent autonomously decides to switch strategies—for instance, triggering the Query Expansion tool if the context is irrelevant due to acronyms. Once a response is generated, a \"response relevance check\" evaluates the answer for completeness and factual accuracy using a stronger model (Claude Sonnet) as a judge. If the quality is insufficient, the agent loops back to try a different retrieval strategy, such as decomposition or further expansion. This feedback loop ensures that the system only returns high-confidence, accurate answers, effectively mimicking a human workflow of verifying information before responding. The session concludes by briefly mentioning other optimization techniques such as semantic chunking, multimodal parsing, hybrid search, and re-ranking to further fine-tune performance."
keywords: Advanced RAG, Self-Corrective RAG, Amazon Bedrock Knowledge Bases, Query Decomposition, Agentic Workflow
---
