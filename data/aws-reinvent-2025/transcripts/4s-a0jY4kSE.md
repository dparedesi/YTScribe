---
video_id: 4s-a0jY4kSE
video_url: https://www.youtube.com/watch?v=4s-a0jY4kSE
is_generated: False
is_translatable: True
summary: This live coding session, presented by AWS Principal Solutions Architects Cy Venom and Lucas Duartz, demonstrates how to streamline EKS operations using agentic AI, building a sophisticated troubleshooting agent that goes beyond simple chatbots to become an autonomous partner capable of reducing mean time to remediation for Kubernetes cluster issues. The session begins by contrasting last year's RAG-based approach, which required manual data chunking, embedding storage in vector databases, periodic updates every 30 minutes to an hour that missed live cluster data, and manual context switching where developers would run kubectl commands and pass results to LLMs, with this year's agentic architecture leveraging modern tools like Strands SDK, MCP servers, and multi-agent patterns. The presenters introduce their Q Agent, an AI agent designed to troubleshoot clusters and remediate issues based on root cause analysis using MCP and agentic capabilities, demonstrating through a recorded demo how it receives alerts from Alert Manager in Slack, automatically identifies issues like monitoring agents stuck in pending state, performs troubleshooting through agentic loops executing tools until finding solutions, consults runbooks from vector databases, and autonomously fixes issues without human intervention, though they acknowledge production deployments should integrate with GitOps for safety. The architecture evolves from a basic setup with an orchestrator agent receiving Slack messages and routing to a specialist agent, initially configured with manually coded tools like describe pod and get pods, to a more intelligent system using the hosted EKS MCP server announced two weeks prior, which eliminates the need for local MCP server deployment, proxy tunneling, and permission management by providing cloud-hosted MCP capabilities leveraging IAM for access control. The demonstration shows how adding MCP transforms the agent from attempting to infer namespaces by parsing pod outputs to directly using MCP-discovered tools like list k8s resources, solving the exponential integration problem where connecting multiple LLMs to numerous tools would require building each integration separately, with MCP serving as a universal integration layer that enables any LLM to communicate with any tool built once. The session then addresses intelligent message classification, replacing static keyword-based filtering with AI-powered intent detection using Nova Micro, a lightweight model that classifies whether Slack messages relate to Kubernetes troubleshooting before invoking the more expensive Cloud Sonnet model, implementing this through Strands' before invocation hooks that intercept messages, use Nova Micro to determine relevance with simple yes or no responses limited to 10 tokens for speed and cost efficiency, demonstrating how this prevents unnecessary agent invocations for casual conversations while correctly identifying technical queries even when specific keywords aren't present. The presentation introduces the concept of tribal knowledge, using the Voyager One probe example where NASA engineers had to retrieve decades-old documentation and consult retired engineers to solve problems, emphasizing how organizational knowledge exists in conversations across Slack, email, and other communication channels, and how this knowledge should be stored in accessible databases for future retrieval. To address this, the architecture expands to include a memory agent powered by S3 Vectors, a vector database announced in July, creating a branching approach where the orchestrator determines whether messages are troubleshooting requests requiring the specialist agent or tips and solutions that should be stored in the memory agent, with the system growing smarter over time as it accumulates solutions from team conversations. The memory agent is built as a standalone microservice using the agent-as-tools pattern, with two primary tools for storing and retrieving solutions from S3 Vectors, using Amazon Titan for embeddings to convert human-readable text into embeddings for efficient storage and retrieval, demonstrating how solutions can be stored manually and then retrieved through semantic search that finds the most relevant solutions based on query similarity. The architecture implements agent-to-agent communication using A2A over HTTP, allowing the orchestrator agent to discover and communicate with specialized agents like the memory agent running as independent servers, with the orchestrator's system prompt updated to always check memory first before troubleshooting, store solutions after successful troubleshooting, and route requests appropriately, demonstrating how breaking monolithic agents into micro-agents improves response quality and performance, similar to the microservices revolution in software architecture. The live demonstration shows the complete workflow where a developer reports a monitoring agent issue in Slack, the agent classifies the message as relevant, searches the memory database finding a stored solution about using the correct node exporter image, determines additional troubleshooting is needed, uses MCP to query live cluster data including pod logs and application logs, identifies the root cause as a non-existent image tag, stores the solution for future use, and when explicitly asked to fix the issue, uses MCP to apply the correct image, successfully resolving the monitoring agent problem. The session emphasizes using the right LLM for each task, with the architecture employing four different models including Cloud Sonnet for main agent reasoning, Nova Micro for fast classification, Titan for embeddings, and potentially different models for different agents, highlighting how this approach is more efficient and cost-effective than using a single powerful model for everything. The presenters conclude by sharing resources including Helm charts for Kubernetes deployment, sample code on GitHub, and emphasizing that the complete architecture combines MCP for live data access, RAG for runbooks and documentation, vector databases for tribal knowledge storage, and multiple specialized agents working together, with the system designed to scale independently as separate microservices that can be deployed as individual pods in Kubernetes, responding to different demand patterns and enabling teams to reduce operational overhead while accelerating troubleshooting and remediation of EKS cluster issues.
keywords: EKS operations, agentic AI, Strands SDK, MCP servers, Kubernetes troubleshooting
---
