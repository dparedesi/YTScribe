---
video_id: PGfyJX8eS48
video_url: https://www.youtube.com/watch?v=PGfyJX8eS48
is_generated: False
is_translatable: True
summary: "This session provides an advanced deep dive into DynamoDB data modeling, focusing on practical techniques for building efficient, scalable database schemas. The presentation begins by establishing foundational concepts, emphasizing DynamoDB's unique characteristics: its fully managed nature with region-wide multi-tenant infrastructure that provides self-healing capabilities and cannot be overloaded like traditional databases; consumption-based pricing model where costs are directly tied to read capacity units, write capacity units, and storage, creating predictable billing and a tight connection between modeling efficiency and cost; and consistent performance at any scale, from megabyte-level to petabyte-level tables. The core architectural concept revolves around partitioning, where data is distributed across multiple partitions based on partition key values, with the request router using hash functions to determine partition placement, enabling constant-time operations regardless of table size as DynamoDB automatically manages partition scaling behind the scenes. The API design provides direct access to items through single-item actions requiring full primary keys for CRUD operations, and query operations for composite primary key tables that allow fetching multiple records with the same partition key, sorted by sort key, with a 1-megabyte limit per request to maintain consistent performance. Secondary indexes enable additional read-based access patterns by creating fully managed copies of data with new primary keys, with global secondary indexes preferred in most cases over local secondary indexes, which have significant constraints and should be used only with deep understanding. A major focus of the session is the recently released multi-attribute composite keys feature for GSIs, which allows up to 4 attributes each for partition and sort keys, eliminating the need for synthetic keys that previously required manually combining attributes into strings like 'warehouse#assignee#status#priority#created_at', which caused item size bloat (sometimes consuming 200 bytes or 20% of a write capacity unit), application complexity, sorting challenges with mixed data types, and difficult update logic. The new feature reuses actual table attributes, eliminates bloat, simplifies updates, enables proper integer sorting, and maintains the same query semantics where all partition key attributes must be specified and sort key ordering follows left-to-right composite index rules. Cost management strategies for secondary indexes include questioning whether an index is truly needed, reusing indexes for multiple use cases when there's high correlation or small search spaces, implementing sparse indexes that only replicate items with all index key attributes to filter data globally, and carefully considering index projections to avoid replicating full items when only specific attributes are needed, which can reduce write costs by 80% and prevent unnecessary updates. The session addresses schema evolution through three main types: adding new unindexed attributes that don't affect data access, which can be handled entirely in application code with default values; adding new indexes on existing attributes, which DynamoDB can backfill automatically; and modifying existing data through backfills, which requires application code updates, scanning items, and using tools like AWS Glue, bulk executor, or Step Functions. The multi-attribute composite keys feature significantly reduces the need for backfills by eliminating synthetic key requirements. Anti-patterns discussed include the kitchen sink item collection where unrelated entity types are grouped together unnecessarily, over-normalized models that don't leverage DynamoDB's denormalization capabilities, hiding the DynamoDB API through abstraction layers that obscure data distribution and pagination, not using the full DynamoDB API for operations like atomic increments, overusing strongly consistent reads when eventually consistent reads save 50% on RCU consumption, having excessively large items that inflate costs, and overusing transaction APIs which should be reserved for low-volume, high-value operations."
keywords: DynamoDB, Data Modeling, Multi-attribute Composite Keys, Secondary Indexes, Schema Evolution
---
