---
video_id: OXNTzhPnJU8
video_url: https://www.youtube.com/watch?v=OXNTzhPnJU8
is_generated: False
is_translatable: True
summary: This comprehensive presentation explores the evolution of Redis from a simple caching solution to a modernized, feature-rich data platform powering real-time applications. Peter Kylo from Redis begins by establishing Redis's position as an in-memory data structure store with 2.25 million daily Docker pulls and recognition as the most loved non-relational database. The talk addresses critical challenges including hot key problems that occur when frequently accessed keys create bottlenecks in single-threaded Redis instances, particularly in scenarios like social media influencer profiles or secondary indexing use cases where sets and sorted sets become heavily accessed. To solve the influencer hot key problem, Redis introduced client-side caching which keeps copies of hot keys in client libraries, enabling local memory access for repeated reads and avoiding network round trips, with Redis tracking cached keys and sending invalidations when writes occur. For secondary indexing challenges, Redis developed the Redis Query Engine (formerly Redis Search) which allows schema definition for indexing fields in hashes or JSON documents, with support for tag, text, numeric, geo, and vector field types, enabling complex queries through a coordinator that performs MapReduce operations across cluster nodes. This query engine unlocks new use cases like session store analytics, online feature stores for machine learning models, and powers blazing-fast vector similarity search capabilities. The presentation introduces Lancache, a semantic caching solution that caches LLM responses and returns semantically similar cached responses instead of calling expensive LLMs, achieving up to 15 times performance improvement and significant cost reduction, with Asurion achieving 70% cache hit rates. The discussion then shifts to caching patterns, explaining cache aside patterns and their limitations including stale data issues and thundering herd problems where cache expiration causes simultaneous database queries. To address cache invalidation complexity, Redis created Redis Data Integration (RDI), a low-code solution built on Apache Flink and Debezium that synchronizes data from relational databases to Redis using change data capture, enabling refresh-ahead caching patterns and CQRS architectures. Access Bank case study demonstrates RDI's impact, reducing response times from 173ms to 29ms and scaling from 500 to tens of thousands of requests per second. To address cost concerns when moving entire datasets to Redis, Redis Flex was introduced with auto-tiering capabilities that extend keyspace with SSD or NVME storage, keeping hot keys in RAM and cold keys on slower storage, reducing costs by up to 75% while supporting databases up to 50 terabytes. The presentation also covers native JSON data structure support with atomic sub-path updates and vector compression reducing memory consumption by up to 92%, making hierarchical data modeling with vector search practical. Performance improvements in Redis 8.4 are highlighted, including over 1.25 million operations per second on a four-core box, up to 90% latency improvements for certain commands, and 2x TLS performance compared to competitors. The talk concludes with Ever Scachecci from iFood sharing real-world implementations: Genplat platform using Redis for rate limiting across 1 trillion tokens per month, internal RAG API using Redis vector store for document retrieval and context delivery to AI models, and Feature Store delivering over 5 million requests per second with nearly 2000 features stored in Redis for low-latency machine learning inference, demonstrating Redis's critical role in powering iFood's massive scale operations across 106 million monthly orders.
keywords: Redis, caching, real-time applications, vector search, data integration
---
